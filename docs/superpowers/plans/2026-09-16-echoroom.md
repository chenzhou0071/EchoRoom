# EchoRoom Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现一个单房间、最多 6 人的实时语音聊天室（类 Oopz）——服务器只做 UDP 语音转发，客户端负责采集/编码/抖动缓冲/解码/混音/播放，并带公屏文字与说话状态高亮。

**Architecture:** Rust workspace 三块：`protocol`（共享二进制协议 crate，客户端/服务端共同依赖）、`server`（std 线程 + 阻塞 socket，TCP 控制 + UDP 转发）、`client`（tauri v2 应用，Rust 后端四条线程处理音频与网络，HTML 前端做 UI）。不用 async，全程 std 线程 + mpsc。

**Tech Stack:** Rust（stable，MSVC 工具链）、tauri v2（客户端 UI）、wasapi 0.15（音频采集/播放，与 LetterSink 同款）、audiopus（libopus 绑定，Opus 编解码）、手写二进制序列化（无 serde）。

## Global Constraints

- 平台：Windows 11（开发与使用）
- 不使用 async 框架：std::thread + std::sync::mpsc 为唯一并发模型
- 协议字节序：**大端**；TCP 分帧 `[len u32][type u8][payload]`；UDP 头 10 字节 `[magic u16=0xEC45][ver u8=1][type u8][uid u16][seq u32]`
- 音频参数（全局固定）：**48000 Hz、单声道、20ms/帧（960 样本）、Opus 40 kbps**
- 端口：TCP 与 UDP 同号，**默认 9000**
- 房间容量：**最多 6 人**（含自己）
- 心跳：客户端每 **2s**；服务器 UDP 映射超时 **15s**；清理扫描每 **5s**
- 抖动缓冲：目标水位 **3 帧**，上限 **8 帧**，缺帧等待 **2 帧（40ms）** 后跳过
- 注释中文、commit 信息英文（与 LetterSink 风格一致）
- 参照实现：WASAPI 采集/播放的事件驱动样板可参照 `E:\pro\captions\src-tauri\src\capture\`（同作者已验证的 wasapi 0.15 用法）

---

## 文件结构（分解锁定）

```
EchoRoom/
├── Cargo.toml                      # workspace：members = ["protocol", "server"]（T7 加 client/src-tauri）
├── protocol/
│   ├── Cargo.toml                  # 零依赖
│   └── src/
│       ├── lib.rs                  # 模块导出与常量
│       ├── messages.rs             # TcpMessage / UdpPacket 枚举定义 + 帧常量
│       ├── tcp.rs                  # TCP 消息编解码（encode / try_decode）
│       └── udp.rs                  # UDP 包头编解码（encode / decode）
├── server/
│   ├── Cargo.toml                  # 依赖 protocol
│   └── src/
│       ├── main.rs                 # 启动：绑定端口，起 TCP/UDP/清理三线程
│       ├── room.rs                 # RoomState：成员表、加入/离开/广播
│       ├── tcp.rs                  # accept 循环 + 每连接读/写线程
│       ├── udp.rs                  # UDP 主循环：register/voice/heartbeat + 转发
│       └── bin/
│           └── sim_clients.rs      # 模拟客户端工具（假客户端 ×N，验证服务器）
└── client/
    ├── ui/                         # 纯静态前端
    │   ├── index.html              # 三区布局：成员网格/公屏/输入框
    │   ├── app.js                  # 桥接逻辑（invoke/emit）
    │   └── style.css               # 深色主题、蓝框高亮
    └── src-tauri/
        ├── Cargo.toml              # 依赖 protocol/tauri/wasapi/audiopus
        ├── tauri.conf.json         # 窗口声明 + 权限（capabilities）
        ├── capabilities/default.json
        ├── icons/                  # 复用 LetterSink 图标即可
        └── src/
            ├── main.rs             # tauri 启动 + 状态注册
            ├── lib.rs              # 模块导出
            ├── bridge.rs           # invoke 命令 + emit 事件
            ├── config.rs           # 昵称/服务器地址持久化（%APPDATA%\com.echoroom.dev\config.json）
            ├── net/
            │   ├── mod.rs
            │   ├── tcp.rs          # TCP 控制客户端：登录/公屏/状态/重连退避
            │   └── udp.rs          # UDP：register/心跳/语音收发
            └── audio/
                ├── mod.rs
                ├── opus.rs         # Opus 编码器/解码器封装
                ├── capture.rs      # WASAPI 麦克风采集（20ms 块）
                ├── playback.rs     # WASAPI 播放（事件驱动，声卡时钟驱动混音）
                ├── jitter.rs       # 每发送者抖动缓冲（排序/水位/缺帧）
                ├── mixer.rs        # 多路 PCM 求和 + 软限幅
                └── vad.rs          # 能量检测 + 双阈值迟滞（说话状态）
```

---

### Task 1: Workspace 骨架

**Files:**
- Create: `Cargo.toml`（workspace 根）
- Create: `protocol/Cargo.toml`
- Create: `protocol/src/lib.rs`
- Create: `server/Cargo.toml`
- Create: `server/src/main.rs`

**Interfaces:**
- Consumes: 无（起点）
- Produces: workspace 可构建；`protocol` crate 可被 `use protocol::...`（T2 起填充内容）；`server` 二进制可 `cargo run -p echoroom-server`

- [ ] **Step 1: 写 workspace 根 Cargo.toml**

```toml
[workspace]
resolver = "2"
members = ["protocol", "server"]

[workspace.package]
version = "0.1.0"
edition = "2021"
```

- [ ] **Step 2: 写 protocol crate 骨架**

`protocol/Cargo.toml`:
```toml
[package]
name = "echoroom-protocol"
version.workspace = true
edition.workspace = true

[dependencies]
```

`protocol/src/lib.rs`:
```rust
//! EchoRoom 共享协议：TCP 控制消息与 UDP 语音包的编解码。
//! 客户端与服务端共同依赖本 crate（单一事实来源）。

/// 默认服务端口（TCP 与 UDP 同号）
pub const DEFAULT_PORT: u16 = 9000;
/// 房间容量（含自己）
pub const ROOM_CAPACITY: usize = 6;
/// 音频帧：20ms @ 48kHz
pub const FRAME_SAMPLES: usize = 960;
/// 采样率
pub const SAMPLE_RATE: u32 = 48000;
/// Opus 目标码率
pub const OPUS_BITRATE: i32 = 40_000;
/// 心跳间隔
pub const HEARTBEAT_INTERVAL_MS: u64 = 2000;
/// UDP 映射超时
pub const UDP_TIMEOUT_MS: u64 = 15_000;
```

- [ ] **Step 3: 写 server 骨架**

`server/Cargo.toml`:
```toml
[package]
name = "echoroom-server"
version.workspace = true
edition.workspace = true

[[bin]]
name = "echoroom-server"
path = "src/main.rs"

[[bin]]
name = "sim_clients"
path = "src/bin/sim_clients.rs"

[dependencies]
echoroom-protocol = { path = "../protocol" }
```

`server/src/main.rs`:
```rust
//! EchoRoom 服务端：TCP 控制 + UDP 语音转发（单房间）。
fn main() {
    let port = std::env::args()
        .nth(1)
        .and_then(|s| s.parse::<u16>().ok())
        .unwrap_or(echoroom_protocol::DEFAULT_PORT);
    println!("echoroom-server listening on port {port} (tcp+udp)");
    // T4 起填充实际逻辑
}
```

`server/src/bin/sim_clients.rs`:
```rust
//! 模拟客户端工具：假客户端 ×N 验证服务器（T6 填充）。
fn main() {
    println!("sim_clients placeholder (T6 实现)");
}
```

- [ ] **Step 4: 构建验证**

Run: `cd E:\pro\EchoRoom; cargo build`
Expected: `Finished` 无错误，两个二进制生成（echoroom-server / sim_clients）

- [ ] **Step 5: Commit**

```bash
git add Cargo.toml protocol server
git commit -m "chore: workspace skeleton with protocol and server crates"
```

---

### Task 2: 协议层——TCP 消息编解码

**Files:**
- Create: `protocol/src/messages.rs`（消息枚举）
- Create: `protocol/src/tcp.rs`（编解码 + 测试）
- Modify: `protocol/src/lib.rs`（加 `pub mod tcp;`）

**Interfaces:**
- Consumes: Task 1 的 crate 骨架
- Produces:
  - `messages::TcpMessage`（枚举，见下）
  - `tcp::encode(msg: &TcpMessage) -> Vec<u8>`
  - `tcp::try_decode(buf: &[u8]) -> Result<Option<(TcpMessage, usize)>, DecodeError>`
    —— `Ok(None)` = 数据不足（等待更多字节）；`Ok(Some((msg, n)))` = 成功解码并消费 n 字节（支持粘包循环）；`Err` = 协议错误（类型未知/UTF-8 非法）
  - `tcp::DecodeError`（枚举：`UnknownType(u8)`、`BadUtf8`）

- [ ] **Step 1: 写消息定义（messages.rs）**

```rust
//! TCP 控制消息定义。类型号固定，客户端与服务端共用。

#[derive(Debug, Clone, PartialEq)]
pub enum TcpMessage {
    /// C→S：进入房间
    Login { nickname: String },
    /// S→C：登录成功（uid、token、已有成员）
    LoginOk { uid: u16, token: u32, members: Vec<(u16, String)> },
    /// S→C：有成员加入
    MemberJoin { uid: u16, nickname: String },
    /// S→C：有成员离开
    MemberLeave { uid: u16 },
    /// C→S（uid 填 0）公屏消息；S→C（uid 为发送者）广播
    Chat { uid: u16, text: String },
    /// C→S（uid 填 0）说话状态；S→C 广播
    Speaking { uid: u16, on: bool },
    /// S→C：房间满等拒绝原因
    LoginReject { reason: String },
}

impl TcpMessage {
    pub fn type_id(&self) -> u8 {
        match self {
            TcpMessage::Login { .. } => 1,
            TcpMessage::LoginOk { .. } => 2,
            TcpMessage::MemberJoin { .. } => 3,
            TcpMessage::MemberLeave { .. } => 4,
            TcpMessage::Chat { .. } => 5,
            TcpMessage::Speaking { .. } => 6,
            TcpMessage::LoginReject { .. } => 7,
        }
    }
}
```

- [ ] **Step 2: 写失败的测试（tcp.rs 内 `#[cfg(test)]`）**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::messages::TcpMessage;

    #[test]
    fn roundtrip_all_variants() {
        let samples = vec![
            TcpMessage::Login { nickname: "阿信".into() },
            TcpMessage::LoginOk { uid: 3, token: 0xDEAD_BEEF, members: vec![(1, "小K".into()), (2, "你".into())] },
            TcpMessage::MemberJoin { uid: 5, nickname: "新来的".into() },
            TcpMessage::MemberLeave { uid: 2 },
            TcpMessage::Chat { uid: 1, text: "晚上开黑吗".into() },
            TcpMessage::Speaking { uid: 4, on: true },
            TcpMessage::LoginReject { reason: "房间已满（6人）".into() },
        ];
        for msg in samples {
            let bytes = encode(&msg);
            let (decoded, n) = try_decode(&bytes).unwrap().unwrap();
            assert_eq!(decoded, msg);
            assert_eq!(n, bytes.len());
        }
    }

    #[test]
    fn partial_data_returns_none() {
        let bytes = encode(&TcpMessage::Login { nickname: "abc".into() });
        for cut in 0..bytes.len() {
            assert!(try_decode(&bytes[..cut]).unwrap().is_none(), "cut={cut} 应等待更多数据");
        }
    }

    #[test]
    fn two_messages_in_one_buffer() {
        let mut buf = encode(&TcpMessage::MemberLeave { uid: 1 });
        buf.extend(encode(&TcpMessage::Speaking { uid: 2, on: false }));
        let (m1, n1) = try_decode(&buf).unwrap().unwrap();
        assert_eq!(m1, TcpMessage::MemberLeave { uid: 1 });
        let (m2, n2) = try_decode(&buf[n1..]).unwrap().unwrap();
        assert_eq!(m2, TcpMessage::Speaking { uid: 2, on: false });
        assert_eq!(n1 + n2, buf.len());
    }

    #[test]
    fn unknown_type_is_error() {
        // 长度 2、类型 99、载荷 1 字节
        let buf = [0u8, 0, 0, 2, 99, 0];
        assert!(matches!(try_decode(&buf), Err(DecodeError::UnknownType(99))));
    }

    #[test]
    fn bad_utf8_is_error() {
        // Login 消息：载荷 = [len u16=2][0xFF 0xFE]（非法 UTF-8）
        let mut payload = vec![0u8, 2, 0xFF, 0xFE];
        let mut buf = ((payload.len() + 1) as u32).to_be_bytes().to_vec();
        buf.push(1); // type = Login
        buf.append(&mut payload);
        assert!(matches!(try_decode(&buf), Err(DecodeError::BadUtf8)));
    }
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd E:\pro\EchoRoom; cargo test -p echoroom-protocol`
Expected: 编译失败（`encode`/`try_decode`/`DecodeError` 未定义）

- [ ] **Step 4: 实现编解码（tcp.rs）**

```rust
//! TCP 控制消息编解码：`[len u32][type u8][payload]`，长度=type+payload 字节数。
//! 载荷手写序列化：u16/u32 大端；String = [len u16][utf8]；bool = u8(0/1)。
use crate::messages::TcpMessage;

#[derive(Debug, PartialEq)]
pub enum DecodeError {
    UnknownType(u8),
    BadUtf8,
}

// ---------- 写 ----------

fn put_str(out: &mut Vec<u8>, s: &str) {
    let b = s.as_bytes();
    out.extend_from_slice(&(b.len() as u16).to_be_bytes());
    out.extend_from_slice(b);
}

pub fn encode(msg: &TcpMessage) -> Vec<u8> {
    let mut payload = Vec::new();
    match msg {
        TcpMessage::Login { nickname } => put_str(&mut payload, nickname),
        TcpMessage::LoginOk { uid, token, members } => {
            payload.extend_from_slice(&uid.to_be_bytes());
            payload.extend_from_slice(&token.to_be_bytes());
            payload.extend_from_slice(&(members.len() as u16).to_be_bytes());
            for (uid, name) in members {
                payload.extend_from_slice(&uid.to_be_bytes());
                put_str(&mut payload, name);
            }
        }
        TcpMessage::MemberJoin { uid, nickname } => {
            payload.extend_from_slice(&uid.to_be_bytes());
            put_str(&mut payload, nickname);
        }
        TcpMessage::MemberLeave { uid } => payload.extend_from_slice(&uid.to_be_bytes()),
        TcpMessage::Chat { uid, text } => {
            payload.extend_from_slice(&uid.to_be_bytes());
            put_str(&mut payload, text);
        }
        TcpMessage::Speaking { uid, on } => {
            payload.extend_from_slice(&uid.to_be_bytes());
            payload.push(if *on { 1 } else { 0 });
        }
        TcpMessage::LoginReject { reason } => put_str(&mut payload, reason),
    }
    let mut out = Vec::with_capacity(5 + payload.len());
    out.extend_from_slice(&((payload.len() + 1) as u32).to_be_bytes());
    out.push(msg.type_id());
    out.extend_from_slice(&payload);
    out
}

// ---------- 读 ----------

struct Reader<'a> {
    buf: &'a [u8],
    pos: usize,
}

impl<'a> Reader<'a> {
    fn need(&self, n: usize) -> Option<()> {
        (self.pos + n <= self.buf.len()).then_some(())
    }
    fn u8(&mut self) -> Option<u8> {
        self.need(1)?;
        let v = self.buf[self.pos];
        self.pos += 1;
        Some(v)
    }
    fn u16(&mut self) -> Option<u16> {
        self.need(2)?;
        let v = u16::from_be_bytes([self.buf[self.pos], self.buf[self.pos + 1]]);
        self.pos += 2;
        Some(v)
    }
    fn u32(&mut self) -> Option<u32> {
        self.need(4)?;
        let v = u32::from_be_bytes(self.buf[self.pos..self.pos + 4].try_into().unwrap());
        self.pos += 4;
        Some(v)
    }
    fn string(&mut self) -> Option<String> {
        let len = self.u16()? as usize;
        self.need(len)?;
        let s = std::str::from_utf8(&self.buf[self.pos..self.pos + len]).ok()?.to_string();
        self.pos += len;
        Some(s)
    }
}

/// 尝试从缓冲区头部解码一条消息。
/// `Ok(None)`=数据不足；`Ok(Some((msg, n)))`=成功（消费 n 字节）；`Err`=协议错误。
pub fn try_decode(buf: &[u8]) -> Result<Option<(TcpMessage, usize)>, DecodeError> {
    if buf.len() < 5 {
        return Ok(None);
    }
    let len = u32::from_be_bytes(buf[0..4].try_into().unwrap()) as usize;
    if len < 1 || len > 64 * 1024 {
        return Err(DecodeError::UnknownType(0)); // 长度异常：视为协议错误
    }
    if buf.len() < 4 + len {
        return Ok(None);
    }
    let type_id = buf[4];
    let body = &buf[5..4 + len];
    let mut r = Reader { buf: body, pos: 0 };

    // 辅助：字段缺失（None）=数据不足？——长度已够时字段缺失属于协议错误；统一按 BadUtf8 上报过于粗糙，
    // 这里约定：字段读失败视为数据不足（调用方会因 len 已满足而不再等待——实际做法见下）。
    macro_rules! field {
        ($e:expr) => {
            match $e {
                Some(v) => v,
                None => return Ok(None),
            }
        };
    }

    let msg = match type_id {
        1 => TcpMessage::Login { nickname: field!(r.string()) },
        2 => {
            let uid = field!(r.u16());
            let token = field!(r.u32());
            let count = field!(r.u16()) as usize;
            let mut members = Vec::with_capacity(count.min(64));
            for _ in 0..count {
                let m_uid = field!(r.u16());
                let name = field!(r.string());
                members.push((m_uid, name));
            }
            TcpMessage::LoginOk { uid, token, members }
        }
        3 => {
            let uid = field!(r.u16());
            TcpMessage::MemberJoin { uid, nickname: field!(r.string()) }
        }
        4 => TcpMessage::MemberLeave { uid: field!(r.u16()) },
        5 => {
            let uid = field!(r.u16());
            TcpMessage::Chat { uid, text: field!(r.string()) }
        }
        6 => {
            let uid = field!(r.u16());
            TcpMessage::Speaking { uid, on: field!(r.u8()) != 0 }
        }
        7 => TcpMessage::LoginReject { reason: field!(r.string()) },
        other => return Err(DecodeError::UnknownType(other)),
    };
    Ok(Some((msg, 4 + len)))
}
```

注意：`string()` 内部非法 UTF-8 返回 None 会与"数据不足"混淆——两个用例（bad_utf8 测试）需要区分。修正做法：给 Reader 加 `Result` 风格：

```rust
enum FieldErr { NotEnough, BadUtf8 }
```
`string()` 返回 `Result<String, FieldErr>`；`u16/u32/u8` 返回 `Result<_, FieldErr>`（只有 NotEnough）。宏：

```rust
macro_rules! field {
    ($e:expr) => {
        match $e {
            Ok(v) => v,
            Err(FieldErr::NotEnough) => return Ok(None),
            Err(FieldErr::BadUtf8) => return Err(DecodeError::BadUtf8),
        }
    };
}
```

（实现时按此写出完整版；测试 `bad_utf8_is_error` 即验证该路径。）

- [ ] **Step 5: 运行测试确认通过**

Run: `cd E:\pro\EchoRoom; cargo test -p echoroom-protocol`
Expected: 5 个测试全部 PASS

- [ ] **Step 6: 更新 lib.rs 并再跑测试**

```rust
pub mod messages;
pub mod tcp;
```

Run: `cargo test -p echoroom-protocol`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add protocol/src/messages.rs protocol/src/tcp.rs protocol/src/lib.rs
git commit -m "feat: tcp message codec with streaming decode"
```

---

### Task 3: 协议层——UDP 包编解码

**Files:**
- Modify: `protocol/src/messages.rs`（追加 `UdpPacket`）
- Create: `protocol/src/udp.rs`（编解码 + 测试）
- Modify: `protocol/src/lib.rs`（加 `pub mod udp;`）

**Interfaces:**
- Consumes: Task 2 的 crate
- Produces:
  - `messages::UdpPacket`（枚举，见下）
  - `udp::encode(uid: u16, seq: u32, pkt: &UdpPacket) -> Vec<u8>`
  - `udp::decode(buf: &[u8]) -> Result<(u16, u32, UdpPacket), udp::DecodeError>`（返回 uid、seq、包）
  - `udp::DecodeError`（`BadMagic`、`BadVersion(u8)`、`UnknownType(u8)`、`TooShort`）

- [ ] **Step 1: 追加 UdpPacket 定义（messages.rs）**

```rust
/// UDP 语音通道包（头 10 字节：magic u16 / ver u8 / type u8 / uid u16 / seq u32）
#[derive(Debug, Clone, PartialEq)]
pub enum UdpPacket {
    /// C→S：绑定地址映射（token 校验）
    Register { token: u32 },
    /// S→C：绑定成功
    RegisterAck,
    /// S→C：绑定失败（token 不匹配）
    RegisterReject,
    /// 语音帧（C→S 发送自己；S→C 转发时 uid 为来源者）
    Voice { opus: Vec<u8> },
    /// 心跳（seq 用于响应观测）
    Heartbeat,
}

impl UdpPacket {
    pub fn type_id(&self) -> u8 {
        match self {
            UdpPacket::Register { .. } => 1,
            UdpPacket::RegisterAck => 2,
            UdpPacket::RegisterReject => 3,
            UdpPacket::Voice { .. } => 4,
            UdpPacket::Heartbeat => 5,
        }
    }
}
```

- [ ] **Step 2: 写失败的测试（udp.rs 内）**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::messages::UdpPacket;

    #[test]
    fn roundtrip_all_variants() {
        let samples = vec![
            UdpPacket::Register { token: 0x1234_5678 },
            UdpPacket::RegisterAck,
            UdpPacket::RegisterReject,
            UdpPacket::Voice { opus: vec![0xAA; 60] },
            UdpPacket::Heartbeat,
        ];
        for (i, pkt) in samples.into_iter().enumerate() {
            let bytes = encode(7, 42 + i as u32, &pkt);
            let (uid, seq, decoded) = decode(&bytes).unwrap();
            assert_eq!(uid, 7);
            assert_eq!(seq, 42 + i as u32);
            assert_eq!(decoded, pkt);
        }
    }

    #[test]
    fn bad_magic_rejected() {
        let mut bytes = encode(1, 0, &UdpPacket::Heartbeat);
        bytes[0] = 0x00;
        assert!(matches!(decode(&bytes), Err(DecodeError::BadMagic)));
    }

    #[test]
    fn bad_version_rejected() {
        let mut bytes = encode(1, 0, &UdpPacket::Heartbeat);
        bytes[2] = 99;
        assert!(matches!(decode(&bytes), Err(DecodeError::BadVersion(99))));
    }

    #[test]
    fn truncated_rejected() {
        let bytes = encode(1, 0, &UdpPacket::Heartbeat);
        assert!(matches!(decode(&bytes[..6]), Err(DecodeError::TooShort)));
    }

    #[test]
    fn voice_payload_preserved() {
        let opus: Vec<u8> = (0..80).map(|i| (i * 3) as u8).collect();
        let bytes = encode(65535, u32::MAX, &UdpPacket::Voice { opus: opus.clone() });
        let (uid, seq, pkt) = decode(&bytes).unwrap();
        assert_eq!((uid, seq), (65535, u32::MAX));
        assert_eq!(pkt, UdpPacket::Voice { opus });
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test -p echoroom-protocol`
Expected: 编译失败（udp 模块未定义）

- [ ] **Step 4: 实现（udp.rs）**

```rust
//! UDP 包编解码。头 10 字节：magic(0xEC45) / ver(1) / type / uid u16 / seq u32，均大端。
use crate::messages::UdpPacket;

pub const MAGIC: u16 = 0xEC45;
pub const VERSION: u8 = 1;
pub const HEADER_LEN: usize = 10;
/// 语音包上限（防御异常大包）
pub const MAX_PACKET: usize = 1200;

#[derive(Debug, PartialEq)]
pub enum DecodeError {
    TooShort,
    BadMagic,
    BadVersion(u8),
    UnknownType(u8),
}

pub fn encode(uid: u16, seq: u32, pkt: &UdpPacket) -> Vec<u8> {
    let mut out = Vec::with_capacity(HEADER_LEN + 8 + 80);
    out.extend_from_slice(&MAGIC.to_be_bytes());
    out.push(VERSION);
    out.push(pkt.type_id());
    out.extend_from_slice(&uid.to_be_bytes());
    out.extend_from_slice(&seq.to_be_bytes());
    match pkt {
        UdpPacket::Register { token } => out.extend_from_slice(&token.to_be_bytes()),
        UdpPacket::Voice { opus } => out.extend_from_slice(opus),
        UdpPacket::RegisterAck | UdpPacket::RegisterReject | UdpPacket::Heartbeat => {}
    }
    out
}

pub fn decode(buf: &[u8]) -> Result<(u16, u32, UdpPacket), DecodeError> {
    if buf.len() < HEADER_LEN {
        return Err(DecodeError::TooShort);
    }
    if buf.len() > MAX_PACKET {
        return Err(DecodeError::TooShort); // 异常大包按不可用丢弃
    }
    let magic = u16::from_be_bytes([buf[0], buf[1]]);
    if magic != MAGIC {
        return Err(DecodeError::BadMagic);
    }
    let ver = buf[2];
    if ver != VERSION {
        return Err(DecodeError::BadVersion(ver));
    }
    let uid = u16::from_be_bytes([buf[4], buf[5]]);
    let seq = u32::from_be_bytes([buf[6], buf[7], buf[8], buf[9]]);
    let payload = &buf[HEADER_LEN..];
    let pkt = match buf[3] {
        1 => {
            if payload.len() < 4 {
                return Err(DecodeError::TooShort);
            }
            UdpPacket::Register { token: u32::from_be_bytes(payload[..4].try_into().unwrap()) }
        }
        2 => UdpPacket::RegisterAck,
        3 => UdpPacket::RegisterReject,
        4 => UdpPacket::Voice { opus: payload.to_vec() },
        5 => UdpPacket::Heartbeat,
        other => return Err(DecodeError::UnknownType(other)),
    };
    Ok((uid, seq, pkt))
}
```

- [ ] **Step 5: 运行测试确认通过 + 更新 lib.rs**

```rust
pub mod messages;
pub mod tcp;
pub mod udp;
```

Run: `cargo test -p echoroom-protocol`
Expected: 10 个测试全部 PASS

- [ ] **Step 6: Commit**

```bash
git add protocol/src/messages.rs protocol/src/udp.rs protocol/src/lib.rs
git commit -m "feat: udp packet codec with magic and version checks"
```

---

### Task 4: 服务端——房间状态（room.rs）

**Files:**
- Create: `server/src/room.rs`（含测试）
- Modify: `server/src/main.rs`（`mod room;` 占位接线）

**Interfaces:**
- Consumes: Task 2/3 的 `echoroom_protocol::{messages::TcpMessage, tcp, ROOM_CAPACITY}`
- Produces:
  - `room::Room`（核心状态，后续 T5/T6 使用）：
    - `Room::new() -> Room`
    - `Room::join(&mut self, nickname: String, tx: Sender<Vec<u8>>) -> Result<JoinOk, JoinErr>`
      - `JoinOk { uid: u16, token: u32, members: Vec<(u16, String)> }`（members=加入前已在的人）
      - `JoinErr::Full`（达 ROOM_CAPACITY）
    - `Room::leave(&mut self, uid: u16) -> bool`（存在则移除）
    - `Room::broadcast(&self, except: Option<u16>, msg: &TcpMessage)`（编码一次，投递给除 except 外所有成员的 tx）
    - `Room::send_to(&self, uid: u16, msg: &TcpMessage)`（定向发送）
    - `Room::set_udp(&mut self, uid: u16, addr: SocketAddr)` / `Room::clear_udp_expired(&mut self, timeout: Duration) -> Vec<u16>`（返回被清理的 uid 列表，用于日志）
    - `Room::member_by_addr(&self, addr: SocketAddr) -> Option<u16>`（UDP 反查）
    - `Room::validate_token(&self, uid: u16, token: u32) -> bool`
    - `Room::others_with_udp(&self, except_uid: u16) -> Vec<SocketAddr>`
    - `Room::nickname(&self, uid: u16) -> Option<String>`

- [ ] **Step 1: 写失败的测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use echoroom_protocol::messages::TcpMessage;
    use std::sync::mpsc;

    #[test]
    fn join_until_capacity_then_reject() {
        let mut room = Room::new();
        let mut rxs = Vec::new();
        for i in 0..ROOM_CAPACITY {
            let (tx, rx) = mpsc::channel();
            let ok = room.join(format!("user{i}"), tx);
            assert!(ok.is_ok());
            rxs.push(rx);
        }
        let (tx, _rx) = mpsc::channel();
        assert_eq!(room.join("第7人".into(), tx), Err(JoinErr::Full));
    }

    #[test]
    fn join_returns_existing_members_and_leaves_removes() {
        let mut room = Room::new();
        let (tx1, _r1) = mpsc::channel();
        let ok1 = room.join("A".into(), tx1).unwrap();
        assert!(ok1.members.is_empty());
        let (tx2, _r2) = mpsc::channel();
        let ok2 = room.join("B".into(), tx2).unwrap();
        assert_eq!(ok2.members, vec![(ok1.uid, "A".to_string())]);
        assert!(room.leave(ok1.uid));
        assert!(!room.leave(ok1.uid)); // 再删为 false
    }

    #[test]
    fn broadcast_skips_except_and_delivers() {
        let mut room = Room::new();
        let (tx1, rx1) = mpsc::channel();
        let (tx2, rx2) = mpsc::channel();
        let a = room.join("A".into(), tx1).unwrap();
        let b = room.join("B".into(), tx2).unwrap();
        room.broadcast(Some(a.uid), &TcpMessage::MemberJoin { uid: b.uid, nickname: "B".into() });
        assert!(rx1.try_recv().is_err(), "A 不应收到（被排除）");
        let bytes = rx2.try_recv().expect("B 应收到");
        let (msg, _) = echoroom_protocol::tcp::try_decode(&bytes).unwrap().unwrap();
        assert_eq!(msg, TcpMessage::MemberJoin { uid: b.uid, nickname: "B".into() });
    }

    #[test]
    fn udp_addr_mapping_and_expiry() {
        use std::time::Duration;
        let mut room = Room::new();
        let (tx, _rx) = mpsc::channel();
        let a = room.join("A".into(), tx).unwrap();
        let addr: std::net::SocketAddr = "127.0.0.1:5555".parse().unwrap();
        assert!(!room.validate_token(a.uid, 999));
        assert!(room.validate_token(a.uid, a.token));
        room.set_udp(a.uid, addr);
        assert_eq!(room.member_by_addr(addr), Some(a.uid));
        assert_eq!(room.others_with_udp(a.uid).len(), 0);
        // 超时 0：立即过期
        let expired = room.clear_udp_expired(Duration::ZERO);
        assert_eq!(expired, vec![a.uid]);
        assert_eq!(room.member_by_addr(addr), None);
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd E:\pro\EchoRoom; cargo test -p echoroom-server`
Expected: 编译失败（`Room` 未定义）

- [ ] **Step 3: 实现 room.rs**

```rust
//! 房间状态：成员表、加入/离开、广播、UDP 地址映射与超时。
use std::collections::HashMap;
use std::net::SocketAddr;
use std::sync::mpsc::Sender;
use std::time::{Duration, Instant};

use echoroom_protocol::messages::TcpMessage;
use echoroom_protocol::{tcp, ROOM_CAPACITY};

pub struct JoinOk {
    pub uid: u16,
    pub token: u32,
    /// 加入前已在房间的成员
    pub members: Vec<(u16, String)>,
}

#[derive(Debug, PartialEq)]
pub enum JoinErr {
    Full,
}

pub struct Member {
    pub uid: u16,
    pub nickname: String,
    pub token: u32,
    pub tx: Sender<Vec<u8>>, // TCP 发送队列（预编码字节）
    pub udp_addr: Option<SocketAddr>,
    pub last_seen: Instant,
}

pub struct Room {
    members: HashMap<u16, Member>,
    next_uid: u16,
    /// 简单确定性伪随机（学习用途：LCG；不引入 rand 依赖）
    rng_state: u64,
}

impl Room {
    pub fn new() -> Room {
        let seed = std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .map(|d| d.as_nanos() as u64)
            .unwrap_or(0x9E3779B97F4A7C15);
        Room { members: HashMap::new(), next_uid: 1, rng_state: seed | 1 }
    }

    fn next_token(&mut self) -> u32 {
        // LCG（Numerical Recipes 常数）
        self.rng_state = self.rng_state.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
        (self.rng_state >> 32) as u32
    }

    pub fn join(&mut self, nickname: String, tx: Sender<Vec<u8>>) -> Result<JoinOk, JoinErr> {
        if self.members.len() >= ROOM_CAPACITY {
            return Err(JoinErr::Full);
        }
        let members: Vec<(u16, String)> =
            self.members.values().map(|m| (m.uid, m.nickname.clone())).collect();
        let uid = self.next_uid;
        self.next_uid = self.next_uid.wrapping_add(1).max(1);
        let token = self.next_token();
        self.members.insert(
            uid,
            Member {
                uid,
                nickname,
                token,
                tx,
                udp_addr: None,
                last_seen: Instant::now(),
            },
        );
        Ok(JoinOk { uid, token, members })
    }

    pub fn leave(&mut self, uid: u16) -> bool {
        self.members.remove(&uid).is_some()
    }

    pub fn broadcast(&self, except: Option<u16>, msg: &TcpMessage) {
        let bytes = tcp::encode(msg);
        for m in self.members.values() {
            if Some(m.uid) == except {
                continue;
            }
            let _ = m.tx.send(bytes.clone()); // 慢客户端只堵自己的队列
        }
    }

    pub fn send_to(&self, uid: u16, msg: &TcpMessage) {
        if let Some(m) = self.members.get(&uid) {
            let _ = m.tx.send(tcp::encode(msg));
        }
    }

    pub fn validate_token(&self, uid: u16, token: u32) -> bool {
        self.members.get(&uid).map(|m| m.token == token).unwrap_or(false)
    }

    pub fn set_udp(&mut self, uid: u16, addr: SocketAddr) {
        if let Some(m) = self.members.get_mut(&uid) {
            m.udp_addr = Some(addr);
            m.last_seen = Instant::now();
        }
    }

    pub fn touch(&mut self, uid: u16) {
        if let Some(m) = self.members.get_mut(&uid) {
            m.last_seen = Instant::now();
        }
    }

    /// 清理超时的 UDP 地址映射（成员保留）；返回被清理的 uid 列表
    pub fn clear_udp_expired(&mut self, timeout: Duration) -> Vec<u16> {
        let now = Instant::now();
        let mut expired = Vec::new();
        for m in self.members.values_mut() {
            if m.udp_addr.is_some() && now.duration_since(m.last_seen) > timeout {
                m.udp_addr = None;
                expired.push(m.uid);
            }
        }
        expired
    }

    pub fn member_by_addr(&self, addr: SocketAddr) -> Option<u16> {
        self.members.values().find(|m| m.udp_addr == Some(addr)).map(|m| m.uid)
    }

    pub fn others_with_udp(&self, except_uid: u16) -> Vec<SocketAddr> {
        self.members
            .values()
            .filter(|m| m.uid != except_uid && m.udp_addr.is_some())
            .filter_map(|m| m.udp_addr)
            .collect()
    }

    pub fn nickname(&self, uid: u16) -> Option<String> {
        self.members.get(&uid).map(|m| m.nickname.clone())
    }
}
```

（`rx_vec` 辅助函数在最终测试里去掉——它只用于示意，保留会编译警告；测试内自建 channel 对即可。）

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test -p echoroom-server`
Expected: 4 个测试全部 PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/room.rs server/src/main.rs
git commit -m "feat: room state with membership, broadcast and udp mapping"
```

---

### Task 5: 服务端——TCP 接入 + 模拟客户端（TCP 部分）

**Files:**
- Create: `server/src/tcp.rs`
- Modify: `server/src/main.rs`（接线：起 TCP 服务）
- Modify: `server/src/bin/sim_clients.rs`（TCP 登录/公屏/接收打印）

**Interfaces:**
- Consumes: `room::Room`（T4）、`protocol::tcp`（T2）
- Produces:
  - `tcp::serve(listener: TcpListener, room: Arc<Mutex<Room>>) -> std::io::Result<()>`（阻塞；每连接 spawn 读线程 + 写线程）
  - `sim_clients` 用法：`cargo run -p echoroom-server --bin sim_clients -- <addr:port> <n> [seconds]`

- [ ] **Step 1: 实现 tcp.rs**

```rust
//! 服务端 TCP：accept 循环 + 每连接读/写线程。
use std::io::{Read, Write};
use std::net::{TcpListener, TcpStream};
use std::sync::{Arc, Mutex};

use echoroom_protocol::messages::TcpMessage;
use echoroom_protocol::tcp;

use crate::room::Room;

pub fn serve(listener: TcpListener, room: Arc<Mutex<Room>>) -> std::io::Result<()> {
    for stream in listener.incoming() {
        match stream {
            Ok(s) => {
                let room = room.clone();
                std::thread::spawn(move || {
                    if let Err(e) = handle_conn(s, room) {
                        eprintln!("[tcp] 连接结束: {e}");
                    }
                });
            }
            Err(e) => eprintln!("[tcp] accept 错误: {e}"),
        }
    }
    Ok(())
}

fn handle_conn(mut stream: TcpStream, room: Arc<Mutex<Room>>) -> std::io::Result<()> {
    let peer = stream.peer_addr()?;
    // ---- 登录（第一条消息必须是 Login）----
    let (nickname, buf_rest) = read_first_login(&mut stream)?;
    let (tx, rx) = std::sync::mpsc::channel::<Vec<u8>>();
    let join = room.lock().unwrap().join(nickname.clone(), tx);
    let ok = match join {
        Ok(ok) => ok,
        Err(_) => {
            let reject = tcp::encode(&TcpMessage::LoginReject { reason: "房间已满（6人）".into() });
            stream.write_all(&reject)?;
            return Ok(());
        }
    };
    println!("[tcp] {nickname}(uid={}) 加入 {peer}", ok.uid);
    // LoginOk（定向）+ MemberJoin（广播给其他人）
    {
        let login_ok = TcpMessage::LoginOk { uid: ok.uid, token: ok.token, members: ok.members.clone() };
        stream.write_all(&tcp::encode(&login_ok))?;
        let room = room.lock().unwrap();
        room.broadcast(Some(ok.uid), &TcpMessage::MemberJoin { uid: ok.uid, nickname: nickname.clone() });
    }
    // ---- 写线程：从 rx 取预编码字节写出 ----
    let mut write_stream = stream.try_clone()?;
    let writer = std::thread::spawn(move || {
        for bytes in rx {
            if write_stream.write_all(&bytes).is_err() {
                break;
            }
        }
    });
    // ---- 读循环 ----
    let mut buf: Vec<u8> = buf_rest;
    let mut chunk = [0u8; 4096];
    'read: loop {
        // 先尝试消费已缓冲数据
        loop {
            match tcp::try_decode(&buf) {
                Ok(Some((msg, n))) => {
                    buf.drain(..n);
                    match msg {
                        TcpMessage::Chat { text, .. } => {
                            let room = room.lock().unwrap();
                            room.broadcast(None, &TcpMessage::Chat { uid: ok.uid, text });
                        }
                        TcpMessage::Speaking { on, .. } => {
                            let room = room.lock().unwrap();
                            room.broadcast(Some(ok.uid), &TcpMessage::Speaking { uid: ok.uid, on });
                        }
                        _ => {}
                    }
                }
                Ok(None) => break,
                Err(e) => {
                    eprintln!("[tcp] uid={} 协议错误: {e:?}", ok.uid);
                    break 'read;
                }
            }
        }
        let n = stream.read(&mut chunk)?;
        if n == 0 {
            break 'read;
        }
        buf.extend_from_slice(&chunk[..n]);
    }
    // ---- 离开 ----
    {
        let mut room = room.lock().unwrap();
        room.leave(ok.uid);
        room.broadcast(None, &TcpMessage::MemberLeave { uid: ok.uid });
    }
    println!("[tcp] {nickname}(uid={}) 离开", ok.uid);
    drop(stream);
    let _ = writer.join();
    Ok(())
}

/// 读缓冲直到解出第一条 Login 消息；返回 (昵称, 剩余未消费字节)
fn read_first_login(stream: &mut TcpStream) -> std::io::Result<(String, Vec<u8>)> {
    let mut buf = Vec::new();
    let mut chunk = [0u8; 1024];
    loop {
        match tcp::try_decode(&buf) {
            Ok(Some((TcpMessage::Login { nickname }, n))) => {
                buf.drain(..n);
                return Ok((nickname, buf));
            }
            Ok(Some(_)) => {
                return Err(std::io::Error::new(std::io::ErrorKind::InvalidData, "首条消息必须是 Login"));
            }
            Ok(None) => {
                let n = stream.read(&mut chunk)?;
                if n == 0 {
                    return Err(std::io::Error::new(std::io::ErrorKind::UnexpectedEof, "连接在登录前关闭"));
                }
                buf.extend_from_slice(&chunk[..n]);
            }
            Err(e) => {
                return Err(std::io::Error::new(std::io::ErrorKind::InvalidData, format!("{e:?}")));
            }
        }
    }
}
```

- [ ] **Step 2: main.rs 接线**

```rust
//! EchoRoom 服务端：TCP 控制 + UDP 语音转发（单房间）。
mod room;
mod tcp;
mod udp;

use std::sync::{Arc, Mutex};

fn main() {
    let port = std::env::args().nth(1).and_then(|s| s.parse::<u16>().ok())
        .unwrap_or(echoroom_protocol::DEFAULT_PORT);
    let room = Arc::new(Mutex::new(room::Room::new()));

    // UDP 转发 + 清理线程（T6 实现；先占位）
    udp::spawn_udp_loop(port, room.clone());
    udp::spawn_cleanup_loop(room.clone());

    let listener = std::net::TcpListener::bind(("0.0.0.0", port)).expect("TCP bind 失败");
    println!("echoroom-server listening on 0.0.0.0:{port} (tcp+udp)");
    tcp::serve(listener, room).expect("TCP 服务异常退出");
}
```

（`udp.rs` 在 T6 创建；为了让本步骤编译通过，T5 先建一个最小 `udp.rs` 占位：）

```rust
//! UDP 语音转发（T6 实现）。
use std::sync::{Arc, Mutex};
use crate::room::Room;

pub fn spawn_udp_loop(_port: u16, _room: Arc<Mutex<Room>>) {}
pub fn spawn_cleanup_loop(_room: Arc<Mutex<Room>>) {}
```

- [ ] **Step 3: sim_clients（TCP 模式）**

```rust
//! 模拟客户端：登录 + 定时公屏 + 打印收到的一切（T6 扩展 UDP）。
use echoroom_protocol::messages::TcpMessage;
use echoroom_protocol::tcp;
use std::io::{Read, Write};
use std::net::TcpStream;

fn main() {
    let args: Vec<String> = std::env::args().collect();
    let addr = args.get(1).cloned().unwrap_or_else(|| "127.0.0.1:9000".into());
    let n: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(2);
    let seconds: u64 = args.get(3).and_then(|s| s.parse().ok()).unwrap_or(10);

    let mut handles = Vec::new();
    for i in 0..n {
        let addr = addr.clone();
        handles.push(std::thread::spawn(move || sim_one(&addr, i, seconds)));
    }
    for h in handles {
        let _ = h.join();
    }
}

fn sim_one(addr: &str, index: usize, seconds: u64) {
    let name = format!("sim{index}");
    let mut stream = TcpStream::connect(addr).expect("连接失败");
    stream.write_all(&tcp::encode(&TcpMessage::Login { nickname: name.clone() })).unwrap();
    let mut buf = Vec::new();
    let mut chunk = [0u8; 4096];
    let start = std::time::Instant::now();
    let mut last_chat = std::time::Instant::now();
    loop {
        if start.elapsed().as_secs() >= seconds {
            break;
        }
        // 定时发公屏
        if last_chat.elapsed().as_millis() >= 1000 {
            let text = format!("{name} 报到 {}", start.elapsed().as_secs());
            stream.write_all(&tcp::encode(&TcpMessage::Chat { uid: 0, text })).unwrap();
            last_chat = std::time::Instant::now();
        }
        // 读（非阻塞式：设短超时）
        stream.set_read_timeout(Some(std::time::Duration::from_millis(100))).unwrap();
        if let Ok(n) = stream.read(&mut chunk) {
            if n == 0 {
                break;
            }
            buf.extend_from_slice(&chunk[..n]);
            while let Ok(Some((msg, used))) = tcp::try_decode(&buf) {
                buf.drain(..used);
                match msg {
                    TcpMessage::Chat { uid, text } => println!("[{name}] 收到 uid={uid}: {text}"),
                    TcpMessage::MemberJoin { uid, nickname } => println!("[{name}] +{nickname}(uid={uid})"),
                    TcpMessage::MemberLeave { uid } => println!("[{name}] -uid={uid}"),
                    other => println!("[{name}] {other:?}"),
                }
            }
        }
    }
    println!("[{name}] 退出");
}
```

- [ ] **Step 4: 手动验证（双终端）**

Run（终端 1）: `cd E:\pro\EchoRoom; cargo run -p echoroom-server`
Run（终端 2）: `cargo run -p echoroom-server --bin sim_clients -- 127.0.0.1:9000 3 8`
Expected: 三名 sim 客户端各自打印其余两人的"报到"消息与成员进出；服务器日志出现加入/离开；8 秒后全退出、服务器打印离开。

- [ ] **Step 5: Commit**

```bash
git add server/src/tcp.rs server/src/main.rs server/src/bin/sim_clients.rs server/src/udp.rs
git commit -m "feat: tcp accept loop with login, chat and speaking broadcast; tcp sim clients"
```

---

### Task 6: 服务端——UDP 转发 + 心跳清理 + sim_clients 语音模拟

**Files:**
- Modify: `server/src/udp.rs`（完整实现替换占位）
- Modify: `server/src/bin/sim_clients.rs`（加 UDP：register/语音/心跳）
- Modify: `server/src/main.rs`（无需改动——已接线）

**Interfaces:**
- Consumes: `room::Room`（T4）、`protocol::udp`（T3）
- Produces:
  - `udp::spawn_udp_loop(port: u16, room: Arc<Mutex<Room>>)`——绑定 UDP 同端口，循环：Register/Voice/Heartbeat
  - `udp::spawn_cleanup_loop(room: Arc<Mutex<Room>>)`——每 5s：`clear_udp_expired(15s)` + 日志
  - `sim_clients` 用法新增第三参含义不变；内部：TCP 登录后用 LoginOk 的 uid/token 做 UDP Register，之后每 20ms 发一包假语音（载荷 = 递增字节），并每 2s 心跳；打印"收到来自 uid=X 的语音 N 包"统计

- [ ] **Step 1: 实现 udp.rs（替换占位）**

```rust
//! UDP 语音转发：Register 校验 / Voice 原样转发 / Heartbeat 刷新 / 超时清理。
use std::net::UdpSocket;
use std::sync::{Arc, Mutex};
use std::time::Duration;

use echoroom_protocol::messages::UdpPacket;
use echoroom_protocol::udp;
use echoroom_protocol::UDP_TIMEOUT_MS;

use crate::room::Room;

pub fn spawn_udp_loop(port: u16, room: Arc<Mutex<Room>>) {
    std::thread::spawn(move || {
        let socket = UdpSocket::bind(("0.0.0.0", port)).expect("UDP bind 失败");
        println!("[udp] listening on 0.0.0.0:{port}");
        let mut buf = [0u8; 2048];
        loop {
            let (n, from) = match socket.recv_from(&mut buf) {
                Ok(v) => v,
                Err(e) => {
                    eprintln!("[udp] recv 错误: {e}");
                    continue;
                }
            };
            let (uid, seq, pkt) = match udp::decode(&buf[..n]) {
                Ok(v) => v,
                Err(e) => {
                    eprintln!("[udp] 丢弃异常包({from}): {e:?}");
                    continue;
                }
            };
            match pkt {
                UdpPacket::Register { token } => {
                    let mut room = room.lock().unwrap();
                    if room.validate_token(uid, token) {
                        room.set_udp(uid, from);
                        socket.send_to(&udp::encode(uid, 0, &UdpPacket::RegisterAck), from).ok();
                        println!("[udp] uid={uid} 注册 {from}");
                    } else {
                        socket.send_to(&udp::encode(uid, 0, &UdpPacket::RegisterReject), from).ok();
                        eprintln!("[udp] uid={uid} token 校验失败（{from}）");
                    }
                }
                UdpPacket::Voice { opus } => {
                    let targets = {
                        let mut room = room.lock().unwrap();
                        // 必须已注册地址：按来源地址反查 uid（防伪造）
                        let real_uid = match room.member_by_addr(from) {
                            Some(u) => u,
                            None => continue,
                        };
                        room.touch(real_uid);
                        room.others_with_udp(real_uid)
                    };
                    // 转发：补来源 uid、保留发送者 seq
                    let out = udp::encode(uid, seq, &UdpPacket::Voice { opus });
                    for t in targets {
                        let _ = socket.send_to(&out, t);
                    }
                }
                UdpPacket::Heartbeat => {
                    let mut room = room.lock().unwrap();
                    if let Some(u) = room.member_by_addr(from) {
                        room.touch(u);
                    }
                }
                UdpPacket::RegisterAck | UdpPacket::RegisterReject => {
                    // 服务器不应收到这两个方向；忽略
                }
            }
        }
    });
}

pub fn spawn_cleanup_loop(room: Arc<Mutex<Room>>) {
    std::thread::spawn(move || loop {
        std::thread::sleep(Duration::from_secs(5));
        let expired = {
            let mut room = room.lock().unwrap();
            room.clear_udp_expired(Duration::from_millis(UDP_TIMEOUT_MS))
        };
        for uid in expired {
            println!("[udp] uid={uid} 地址映射超时清理");
        }
    });
}
```

- [ ] **Step 2: sim_clients 扩展（在 sim_one 里追加 UDP）**

```rust
// —— 在收到 LoginOk 后（match other 分支中处理）——
// 记录 (uid, token)，建立 UDP socket 并注册；之后循环里每 20ms 发一包假语音；
// 收到 Voice 时统计来源 uid 的包数。
//
// 具体实现要点（完整代码见文件）：
// 1) match 中拦截 TcpMessage::LoginOk { uid, token, .. } => { my_uid = uid; my_token = token;
//    udp_sock = UdpSocket::bind("0.0.0.0:0").unwrap();
//    udp_sock.connect(addr)?;    // 记录服务器 UDP 地址（与 TCP 同 host:port）
//    udp_sock.send(&udp::encode(uid, 0, &UdpPacket::Register { token })).unwrap(); }
// 2) 主循环按 20ms 间隔发送 UdpPacket::Voice { opus: vec![byte; 60] }，seq 自增；
//    每 2s 发一次 Heartbeat；
// 3) 主循环里用 udp_sock.recv_from 非阻塞收集：统计 HashMap<u16, u64> 每来源包数（uid 从包头的 decode 返回）；
// 4) 退出前打印每来源收包统计与"自己发出"计数。
```

（实现时把上述要点落成实际代码——结构同 TCP 部分：一个 loop 内定时发送 + 收包统计。）

- [ ] **Step 3: 运行测试 + 手动验证（三终端）**

Run: `cargo test`（protocol/server 全部通过）
Run（终端 1）: `cargo run -p echoroom-server`
Run（终端 2）: `cargo run -p echoroom-server --bin sim_clients -- 127.0.0.1:9000 3 12`
Expected:
- 服务器日志：3 个 uid 注册成功
- 模拟客户端：每人统计到来自其他 2 人的语音包（12 秒 × 50 包/秒 ≈ 600 包/人/来源，数量级正确即可）
- 等 12 秒退出后再看服务器：连接的离开日志；**等 15 秒**再看：`地址映射超时清理` 日志（因为 TCP 断开时映射已随成员移除——超时清理主要覆盖"TCP 挂着但 UDP 掉线"的场景；可在模拟中单独验证：让某个 sim 只停发 UDP——作为可选验证）

- [ ] **Step 4: Commit**

```bash
git add server/src/udp.rs server/src/bin/sim_clients.rs
git commit -m "feat: udp voice forwarding with register, heartbeat and expiry cleanup"
```

---

### Task 7: 客户端——tauri 骨架 + 配置 + UI 壳

**Files:**
- Create: `client/src-tauri/Cargo.toml`、`tauri.conf.json`、`capabilities/default.json`、`src/main.rs`、`src/lib.rs`、`src/config.rs`
- Create: `client/ui/index.html`、`app.js`、`style.css`
- Modify: `Cargo.toml`（workspace members 加 `client/src-tauri`）
- 图标：从 `E:\pro\captions\src-tauri\icons\` 复制（复用 LetterSink 图标；用户可后续替换）

**Interfaces:**
- Consumes: Task 1 的 workspace
- Produces:
  - tauri 应用可 `cargo tauri dev` 启动（单窗口）
  - `config::Config { nickname: String, server_addr: String }`、`config::default_config_path()`（`%APPDATA%\com.echoroom.dev\config.json`，**构建期加载模式**——沿用 LetterSink 的竞态修复方案）
  - UI 静态结构：成员网格（假数据 3 人）+ 公屏（假消息）+ 输入框（禁用）

- [ ] **Step 1: workspace 加成员**

根 `Cargo.toml`：`members = ["protocol", "server", "client/src-tauri"]`

- [ ] **Step 2: client/src-tauri 骨架（参照 LetterSink 的 tauri v2 配置）**

`Cargo.toml`:
```toml
[package]
name = "echoroom-client"
version.workspace = true
edition.workspace = true

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
echoroom-protocol = { path = "../../protocol" }

[lib]
name = "echoroom_client_lib"
path = "src/lib.rs"

[[bin]]
name = "echoroom-client"
path = "src/main.rs"
```

`tauri.conf.json`（单窗口、深色、标题 EchoRoom；`frontendDist: "../../ui"`；bundle 先不配）：
```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "EchoRoom",
  "version": "0.1.0",
  "identifier": "com.echoroom.dev",
  "build": { "frontendDist": "../../ui" },
  "app": {
    "withGlobalTauri": true,
    "windows": [
      {
        "label": "main",
        "title": "EchoRoom",
        "width": 420,
        "height": 640,
        "resizable": true
      }
    ],
    "security": { "csp": null }
  }
}
```

`capabilities/default.json`:
```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力",
  "windows": ["main"],
  "permissions": ["core:default"]
}
```

`src/main.rs`:
```rust
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    echoroom_client_lib::run();
}
```

`src/lib.rs`:
```rust
pub fn run() {
    tauri::Builder::default()
        .run(tauri::generate_context!())
        .expect("error while running EchoRoom");
}
```

`build.rs`:
```rust
fn main() {
    tauri_build::build()
}
```

- [ ] **Step 3: UI 壳（三区布局静态版）**

`index.html` 结构（深色主题，三区：成员网格 / 公屏 / 输入行），`app.js` 用假数据渲染 3 个成员卡与几条消息，`style.css` 定义 `.member.speaking { border-color: #3b82f6; }`（蓝框）等基础样式。要点：
- 成员网格：`display:grid; grid-template-columns: repeat(3, 1fr); overflow-y:auto;`（一行 3 个、超出滚动）
- 成员卡：昵称居中；`.speaking` 蓝框
- 公屏：`flex:1; overflow-y:auto;` 消息列表
- 输入行：`input + button`
- 顶栏：标题 + 连接状态占位

- [ ] **Step 4: config.rs（构建期加载，无竞态）**

```rust
//! 客户端配置：昵称与服务器地址，持久化到 %APPDATA%\com.echoroom.dev\config.json。
use serde::{Deserialize, Serialize};
use std::path::PathBuf;

pub const APP_IDENTIFIER: &str = "com.echoroom.dev";

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct Config {
    pub nickname: String,
    pub server_addr: String,
}

impl Default for Config {
    fn default() -> Self {
        Config { nickname: String::new(), server_addr: "127.0.0.1:9000".into() }
    }
}

pub fn default_config_path() -> PathBuf {
    let base = std::env::var("APPDATA").unwrap_or_else(|_| ".".into());
    PathBuf::from(base).join(APP_IDENTIFIER).join("config.json")
}

impl Config {
    pub fn load(path: &std::path::Path) -> Config {
        std::fs::read_to_string(path)
            .ok()
            .and_then(|s| serde_json::from_str(&s).ok())
            .unwrap_or_default()
    }
    pub fn save(&self, path: &std::path::Path) -> std::io::Result<()> {
        if let Some(dir) = path.parent() {
            std::fs::create_dir_all(dir)?;
        }
        std::fs::write(path, serde_json::to_string_pretty(self).unwrap())
    }
}
```

（`Cargo.toml` 依赖补 `serde = { version = "1", features = ["derive"] }`、`serde_json = "1"`）

- [ ] **Step 5: 启动验证**

Run: `cd E:\pro\EchoRoom; cargo tauri dev`（工作目录 `client/src-tauri`——或直接在 `client/src-tauri` 下运行）
Expected: EchoRoom 窗口出现：顶栏 + 3 个假成员卡 + 假公屏 + 输入行（禁用状态）

- [ ] **Step 6: Commit**

```bash
git add Cargo.toml client
git commit -m "feat: client tauri skeleton with static ui shell and config"
```

---

### Task 8: 客户端——TCP 网络层 + 桥接（公屏文字闭环）

**Files:**
- Create: `client/src-tauri/src/net/mod.rs`、`net/tcp.rs`
- Create: `client/src-tauri/src/bridge.rs`
- Modify: `client/src-tauri/src/lib.rs`（注册状态与命令）、`main.rs`
- Modify: `client/ui/app.js`（真实数据渲染：成员/公屏/连接状态）

**Interfaces:**
- Consumes: `protocol::tcp`（T2）、`config::Config`（T7）
- Produces:
  - `net::tcp::spawn(cfg: NetConfig, bridge: Bridge) -> NetHandle`
    - `Bridge`（UI 事件出口）：`bridge.emit_member_list(Vec<(u16,String)>)`、`emit_member_join(u16,String)`、`emit_member_leave(u16)`、`emit_chat(u16,String)`、`emit_speaking(u16,bool)`、`emit_conn(ConnState)`（`ConnState::{Connecting,Connected,Reconnecting}`）
    - `NetHandle { tx: Sender<NetCmd> }`；`NetCmd::SendChat(String)`、`NetCmd::SetSpeaking(bool)`
  - tauri 命令（bridge.rs）：`get_config`、`set_config(nickname, server_addr)`、`send_chat(text)`、`get_login_state`
  - UI 事件名（emit）：`"members"`、`"chat"`、`"speaking"`、`"conn"`
  - 重连退避：断开后 1s → 2s → 5s → 10s（封顶 10s），期间 `Reconnecting`

- [ ] **Step 1: 实现 net/tcp.rs**

```rust
//! TCP 控制客户端：登录、公屏、成员与说话状态事件；断线自动重连（退避）。
use std::io::{Read, Write};
use std::net::TcpStream;
use std::sync::mpsc::{Receiver, Sender};
use std::time::Duration;

use echoroom_protocol::messages::TcpMessage;
use echoroom_protocol::tcp;

use crate::bridge::{Bridge, ConnState};

pub enum NetCmd {
    SendChat(String),
    SetSpeaking(bool),
    Shutdown,
}

pub struct NetHandle {
    pub tx: Sender<NetCmd>,
    pub my_uid: std::sync::Arc<std::sync::atomic::AtomicU16>,
    pub my_token: std::sync::Arc<std::sync::atomic::AtomicU32>,
}

pub fn spawn(addr: String, nickname: String, bridge: Bridge) -> NetHandle {
    let (tx, rx) = std::sync::mpsc::channel::<NetCmd>();
    let my_uid = std::sync::Arc::new(std::sync::atomic::AtomicU16::new(0));
    let my_token = std::sync::Arc::new(std::sync::atomic::AtomicU32::new(0));
    let (uid_c, tok_c) = (my_uid.clone(), my_token.clone());
    std::thread::spawn(move || run_loop(addr, nickname, bridge, rx, uid_c, tok_c));
    NetHandle { tx, my_uid, my_token }
}

fn run_loop(
    addr: String,
    nickname: String,
    bridge: Bridge,
    rx: Receiver<NetCmd>,
    my_uid: std::sync::Arc<std::sync::atomic::AtomicU16>,
    my_token: std::sync::Arc<std::sync::atomic::AtomicU32>,
) {
    let mut backoff = [1u64, 2, 5, 10];
    let mut attempt = 0usize;
    loop {
        bridge.emit_conn(if attempt == 0 { ConnState::Connecting } else { ConnState::Reconnecting });
        match TcpStream::connect(&addr) {
            Ok(mut stream) => {
                attempt = 0;
                if let Err(e) = run_session(&mut stream, &nickname, &bridge, &rx, &my_uid, &my_token) {
                    eprintln!("[net] 会话结束: {e}");
                }
                // 会话结束（断开）：重连前清空 UI 成员态
                bridge.emit_conn(ConnState::Reconnecting);
            }
            Err(e) => {
                eprintln!("[net] 连接失败: {e}");
                bridge.emit_conn(ConnState::Reconnecting);
            }
        }
        let wait = backoff[attempt.min(backoff.len() - 1)];
        attempt += 1;
        std::thread::sleep(Duration::from_secs(wait));
        // Shutdown 检查（非阻塞）
        if let Ok(NetCmd::Shutdown) = rx.try_recv() {
            return;
        }
    }
}

fn run_session(
    stream: &mut TcpStream,
    nickname: &str,
    bridge: &Bridge,
    rx: &Receiver<NetCmd>,
    my_uid: &std::sync::atomic::AtomicU16,
    my_token: &std::sync::atomic::AtomicU32,
) -> std::io::Result<()> {
    stream.write_all(&tcp::encode(&TcpMessage::Login { nickname: nickname.into() }))?;
    let mut buf = Vec::new();
    let mut chunk = [0u8; 4096];
    stream.set_read_timeout(Some(Duration::from_millis(50)))?;
    loop {
        // 发（非阻塞轮询命令）
        while let Ok(cmd) = rx.try_recv() {
            match cmd {
                NetCmd::SendChat(text) => {
                    stream.write_all(&tcp::encode(&TcpMessage::Chat { uid: 0, text }))?;
                }
                NetCmd::SetSpeaking(on) => {
                    stream.write_all(&tcp::encode(&TcpMessage::Speaking { uid: 0, on }))?;
                }
                NetCmd::Shutdown => {
                    return Ok(());
                }
            }
        }
        // 收
        match stream.read(&mut chunk) {
            Ok(0) => return Ok(()),                          // 服务端关闭
            Ok(n) => buf.extend_from_slice(&chunk[..n]),     // 继续解码（见下）
            Err(e) if e.kind() == std::io::ErrorKind::WouldBlock
                || e.kind() == std::io::ErrorKind::TimedOut => {} // 超时：继续轮询
            Err(e) => return Err(e),
        }
        while let Ok(Some((msg, used))) = tcp::try_decode(&buf) {
            buf.drain(..used);
            match msg {
                TcpMessage::LoginOk { uid, token, members } => {
                    my_uid.store(uid, std::sync::atomic::Ordering::Relaxed);
                    my_token.store(token, std::sync::atomic::Ordering::Relaxed);
                    bridge.emit_conn(ConnState::Connected);
                    bridge.emit_member_list(members);
                }
                TcpMessage::LoginReject { reason } => {
                    bridge.emit_conn(ConnState::Rejected(reason));
                    return Ok(());
                }
                TcpMessage::MemberJoin { uid, nickname } => bridge.emit_member_join(uid, nickname),
                TcpMessage::MemberLeave { uid } => bridge.emit_member_leave(uid),
                TcpMessage::Chat { uid, text } => bridge.emit_chat(uid, text),
                TcpMessage::Speaking { uid, on } => bridge.emit_speaking(uid, on),
                _ => {}
            }
        }
    }
}
```

（`ConnState` 加入 `Rejected(String)` 变体；`Bridge::emit_conn` 把状态序列化成 UI 事件字符串："connecting"/"connected"/"reconnecting"/`"rejected:<reason>"`。）

- [ ] **Step 2: 实现 bridge.rs（tauri 命令 + 事件出口）**

```rust
//! tauri 桥接：UI 命令（invoke）与事件出口（emit）。
use std::sync::{Arc, Mutex};
use tauri::{AppHandle, Emitter, Manager, State};

use crate::config::{Config, default_config_path};
use crate::net::tcp::{self, NetCmd, NetHandle};

#[derive(Clone, PartialEq)]
pub enum ConnState {
    Connecting,
    Connected,
    Reconnecting,
    Rejected(String),
}

/// 事件出口：net 线程 → UI
#[derive(Clone)]
pub struct Bridge {
    pub app: AppHandle,
}

impl Bridge {
    pub fn emit_member_list(&self, members: Vec<(u16, String)>) {
        let _ = self.app.emit("members", members);
    }
    pub fn emit_member_join(&self, uid: u16, nickname: String) {
        let _ = self.app.emit("member_join", (uid, nickname));
    }
    pub fn emit_member_leave(&self, uid: u16) {
        let _ = self.app.emit("member_leave", uid);
    }
    pub fn emit_chat(&self, uid: u16, text: String) {
        let _ = self.app.emit("chat", serde_json::json!({"uid": uid, "text": text}));
    }
    pub fn emit_speaking(&self, uid: u16, on: bool) {
        let _ = self.app.emit("speaking", serde_json::json!({"uid": uid, "on": on}));
    }
    pub fn emit_conn(&self, state: ConnState) {
        let s = match state {
            ConnState::Connecting => "connecting".to_string(),
            ConnState::Connected => "connected".to_string(),
            ConnState::Reconnecting => "reconnecting".to_string(),
            ConnState::Rejected(r) => format!("rejected:{r}"),
        };
        let _ = self.app.emit("conn", s);
    }
}

/// 应用状态
pub struct AppState {
    pub config: Mutex<Config>,
    pub net: Mutex<Option<NetHandle>>,
}

// ---- 命令 ----

#[tauri::command]
pub fn get_config(state: State<AppState>) -> Config {
    state.config.lock().unwrap().clone()
}

#[tauri::command]
pub fn set_config(app: AppHandle, state: State<AppState>, nickname: String, server_addr: String) -> Result<(), String> {
    {
        let mut cfg = state.config.lock().unwrap();
        cfg.nickname = nickname.clone();
        cfg.server_addr = server_addr.clone();
        let _ = cfg.save(&default_config_path());
    }
    // 重连以应用新配置
    connect_with_app(&app, nickname, server_addr);
    Ok(())
}

#[tauri::command]
pub fn connect(app: AppHandle, state: State<AppState>) -> Result<(), String> {
    let cfg = state.config.lock().unwrap().clone();
    if cfg.nickname.is_empty() {
        return Err("请先设置昵称".into());
    }
    connect_with_app(&app, cfg.nickname, cfg.server_addr);
    Ok(())
}

/// 用（新的）配置发起连接：停掉旧会话、spawn 新会话。UI 命令与启动自动连接共用。
pub fn connect_with_app(app: &AppHandle, nickname: String, addr: String) {
    let bridge = Bridge { app: app.clone() };
    let handle = tcp::spawn(addr, nickname, bridge);
    let state = app.state::<AppState>();
    let mut slot = state.net.lock().unwrap();
    if let Some(old) = slot.take() {
        let _ = old.tx.send(NetCmd::Shutdown);
    }
    *slot = Some(handle);
}

#[tauri::command]
pub fn send_chat(state: State<AppState>, text: String) -> Result<(), String> {
    let slot = state.net.lock().unwrap();
    let h = slot.as_ref().ok_or("未连接")?;
    h.tx.send(NetCmd::SendChat(text)).map_err(|e| e.to_string())
}
```

- [ ] **Step 3: lib.rs 注册**

```rust
pub mod bridge;
pub mod config;
pub mod net;

use bridge::AppState;

pub fn run() {
    let cfg = config::Config::load(&config::default_config_path());
    tauri::Builder::default()
        .manage(AppState { config: std::sync::Mutex::new(cfg), net: std::sync::Mutex::new(None) })
        .invoke_handler(tauri::generate_handler![
            bridge::get_config,
            bridge::set_config,
            bridge::connect,
            bridge::send_chat
        ])
        .setup(|app| {
            // 启动后自动连接（配置已有昵称时；connect_with_app 内部只 spawn 线程，不阻塞启动）
            let cfg = config::Config::load(&config::default_config_path());
            if !cfg.nickname.is_empty() {
                bridge::connect_with_app(app.handle(), cfg.nickname, cfg.server_addr);
            }
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("error while running EchoRoom");
}
```

- [ ] **Step 4: app.js 接真实数据**

要点：
- 监听 `members`/`member_join`/`member_leave`/`chat`/`speaking`/`conn` 事件；
- 成员卡片：`data-uid` 属性；`speaking` 事件 → 对应卡片加/去 `.speaking` 类（蓝框）；
- 公屏：收到 `chat` → 追加消息行（`昵称: 文本`，自己显示"我"）；
- `conn` → 顶栏状态文本（connecting/connected/reconnecting/rejected:xxx）；
- 首次启动（无昵称）：显示昵称+地址输入小面板 → `set_config` 保存并自动连接；
- 输入框 Enter/发送 → `invoke('send_chat', { text })`。

- [ ] **Step 5: 本机联调验证**

Run（终端 1）: `cargo run -p echoroom-server`
Run（终端 2）: `cargo tauri dev`（client/src-tauri 下）
Run（终端 3）: `cargo run -p echoroom-server --bin sim_clients -- 127.0.0.1:9000 2 20`
Expected:
- 客户端 UI：成员格出现 sim0/sim1 与你；公屏滚动显示 sim 的"报到"消息；顶栏"已连接"
- 客户端发消息 → 终端 3 的 sim 打印收到
- sim 退出（20 秒后）→ UI 成员格移除对应卡片

- [ ] **Step 6: Commit**

```bash
git add client/src-tauri client/ui
git commit -m "feat: client tcp network with bridge, reconnection and chat ui"
```

---

### Task 9: 音频——Opus 编解码封装

**Files:**
- Create: `client/src-tauri/src/audio/mod.rs`（`pub mod opus;`）
- Create: `client/src-tauri/src/audio/opus.rs`（含测试）
- Modify: `client/src-tauri/Cargo.toml`（依赖 `audiopus`、`anyhow`）、`client/src-tauri/src/lib.rs`（`pub mod audio;`）

**Interfaces:**
- Consumes: `protocol::{FRAME_SAMPLES, OPUS_BITRATE}`（T1）
- Produces:
  - `audio::opus::OpusEnc { pub fn new() -> Result<OpusEnc, audiopus::Error>; pub fn encode(&mut self, pcm: &[i16]) -> Result<Vec<u8>, audiopus::Error> }`（pcm 长度 = 960）
  - `audio::opus::OpusDec { pub fn new() -> Result<...>; pub fn decode(&mut self, frame: Option<&[u8]>, out: &mut [i16]) -> Result<usize, audiopus::Error> }`（`None` = 丢包隐藏 PLC；out 长度 = 960；返回实际解码样本数）

- [ ] **Step 0: 前置检查——cmake（audiopus 构建依赖）**

Run: `cmake --version`
Expected: 版本号。**若命令不存在**：`winget install Kitware.CMake`（或使用 VS 自带的 cmake），重新打开终端后再验证。

- [ ] **Step 1: 加依赖**

`client/src-tauri/Cargo.toml` 的 `[dependencies]` 追加：
```toml
audiopus = "0.3"
anyhow = "1"
```

- [ ] **Step 2: 写失败的测试（audio/opus.rs 内）**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn sine_440() -> Vec<i16> {
        let mut v = Vec::with_capacity(960);
        for i in 0..960 {
            let t = i as f32 / 48000.0;
            v.push((f32::sin(2.0 * std::f32::consts::PI * 440.0 * t) * 12000.0) as i16);
        }
        v
    }

    fn rms(pcm: &[i16]) -> f64 {
        if pcm.is_empty() {
            return 0.0;
        }
        let s: f64 = pcm.iter().map(|&x| (x as f64) * (x as f64)).sum();
        (s / pcm.len() as f64).sqrt()
    }

    #[test]
    fn encode_decode_roundtrip_preserves_signal() {
        let input = sine_440();
        let mut enc = OpusEnc::new().unwrap();
        let mut dec = OpusDec::new().unwrap();
        let packet = enc.encode(&input).unwrap();
        assert!(!packet.is_empty(), "编码输出不应为空");
        let mut out = vec![0i16; 960];
        let n = dec.decode(Some(&packet), &mut out).unwrap();
        assert_eq!(n, 960, "解码应输出整帧");
        // 正弦 440Hz 经 Opus 后能量同量级（保守阈值：>30%）
        assert!(rms(&out) > rms(&input) * 0.3, "能量过度衰减: {} vs {}", rms(&out), rms(&input));
    }

    #[test]
    fn plc_decode_does_not_panic() {
        let mut dec = OpusDec::new().unwrap();
        let mut out = vec![0i16; 960];
        let n = dec.decode(None, &mut out).unwrap();
        assert_eq!(n, 960);
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `cd E:\pro\EchoRoom\client\src-tauri; cargo test`
Expected: 编译失败（OpusEnc/OpusDec 未定义）。**注**：首次构建会编译 audiopus/libopus（需要 cmake，可能耗时 1~3 分钟）。

- [ ] **Step 4: 实现（audio/opus.rs）**

```rust
//! Opus 编解码封装（48kHz 单声道，20ms 帧 = 960 样本）。
use audiopus::coder::{Decoder, Encoder};
use audiopus::{Application, Bitrate, Channels, SampleRate};

use echoroom_protocol::{FRAME_SAMPLES, OPUS_BITRATE};

/// 编码器：麦克风 PCM → Opus 包
pub struct OpusEnc(Encoder);

impl OpusEnc {
    pub fn new() -> Result<Self, audiopus::Error> {
        let mut e = Encoder::new(SampleRate::Hz48000, Channels::Mono, Application::Voip)?;
        e.set_bitrate(Bitrate::BitsPerSecond(OPUS_BITRATE))?;
        Ok(Self(e))
    }

    /// 编码一帧（pcm.len() 必须为 FRAME_SAMPLES）
    pub fn encode(&mut self, pcm: &[i16]) -> Result<Vec<u8>, audiopus::Error> {
        debug_assert_eq!(pcm.len(), FRAME_SAMPLES);
        let mut out = vec![0u8; 512]; // 40kbps×20ms≈100B，512 有充足余量
        let n = self.0.encode(pcm, &mut out)?;
        out.truncate(n);
        Ok(out)
    }
}

/// 解码器：Opus 包 → PCM（None 触发丢包隐藏 PLC）
pub struct OpusDec(Decoder);

impl OpusDec {
    pub fn new() -> Result<Self, audiopus::Error> {
        Ok(Self(Decoder::new(SampleRate::Hz48000, Channels::Mono)?))
    }

    /// 解码一帧到 out（长度须为 FRAME_SAMPLES）；返回解码样本数（正常 = 960）
    pub fn decode(&mut self, frame: Option<&[u8]>, out: &mut [i16]) -> Result<usize, audiopus::Error> {
        debug_assert_eq!(out.len(), FRAME_SAMPLES);
        self.0.decode(frame, out, false)
    }
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cargo test`
Expected: 2 个测试 PASS（roundtrip + PLC）

- [ ] **Step 6: Commit**

```bash
git add client/src-tauri/Cargo.toml client/src-tauri/src/audio client/src-tauri/src/lib.rs
git commit -m "feat: opus encoder/decoder wrapper with plc support"
```

---

### Task 10: 音频——WASAPI 麦克风采集（20ms 块）

**Files:**
- Create: `client/src-tauri/src/audio/capture.rs`
- Create: `client/src-tauri/src/bin/mic_probe.rs`（验证工具）
- Modify: `client/src-tauri/src/audio/mod.rs`（`pub mod capture;`）、`Cargo.toml`（`wasapi = "0.15"`）

**Interfaces:**
- Consumes: `protocol::{SAMPLE_RATE, FRAME_SAMPLES}`（T1）
- Produces:
  - `audio::capture::MicCapture::open() -> anyhow::Result<MicCapture>`（默认麦克风；请求 48kHz/单声道/i16；若设备格式采样率非 48000 则返回错误并提示）
  - `MicCapture::pump(&self, out: &mut Vec<i16>) -> anyhow::Result<usize>`（等待声卡事件，读出可用样本追加到 out，返回追加数量）
  - 验证工具：`cargo run --bin mic_probe`

**实现提示**：事件驱动的 WASAPI 采集样板与 `E:\pro\captions\src-tauri\src\capture\system.rs` 同构——`initialize_mta()` → `get_default_device(&Direction::Capture)` → `initialize_client(&format, &share_mode, &stream_flags)` → `get_audiocaptureclient()` → `set_get_eventhandle()` → `start_stream()` → 循环 `wait_for_event` + `get_next_packet_size` / `read_from_device`。要点差异：本任务请求 `SampleType::Int16`、`Channels::Mono`、`SampleRate::Hz48000`（WASAPI 共享模式会自动转换），读到的样本直接进 `Vec<i16>`。

- [ ] **Step 1: 实现 capture.rs**

结构（实现时按上述样板展开完整代码）：
```rust
//! 麦克风采集：默认设备、48kHz 单声道 i16、事件驱动。
pub struct MicCapture {
    audio_client: wasapi::AudioClient,
    capture_client: wasapi::AudioCaptureClient,
    _event: wasapi::Handle, // 保持事件句柄存活
    sample_rate: u32,
}

impl MicCapture {
    pub fn open() -> anyhow::Result<MicCapture> { /* 见上方实现提示 */ }

    /// 阻塞等待事件并读出所有可用样本（追加到 out）；返回本次追加的样本数。
    pub fn pump(&self, out: &mut Vec<i16>) -> anyhow::Result<usize> {
        self.audio_client.wait_for_event()?;
        let mut total = 0usize;
        loop {
            let nbr = match self.audio_client.get_next_packet_size()? {
                Some(n) => n,
                None => break,
            };
            if nbr == 0 {
                break;
            }
            let mut chunk: Vec<i16> = Vec::new();
            let (frames, _flags) = self.capture_client.read_from_device::<i16>(&mut chunk)?;
            if frames > 0 {
                out.extend_from_slice(&chunk);
                total += chunk.len();
            }
        }
        Ok(total)
    }
}
```

- [ ] **Step 2: mic_probe 工具**

```rust
//! 麦克风采集探针：每秒打印采集样本数与 RMS 能量（说话时明显增大）。
fn main() {
    // 初始化：MicCapture::open()
    // 循环：accumulate 到每秒统计一次 —— 打印 "samples=48000 rms=1234"
}
```

（要点：每秒窗口统计样本数 + RMS；Ctrl+C 退出；预期每秒约 48000 样本。）

- [ ] **Step 3: 验证**

Run: `cd E:\pro\EchoRoom\client\src-tauri; cargo run --bin mic_probe`
Expected: 每秒输出 `samples≈48000`；对着麦克风说话时 rms 显著上升，静音时 rms 接近 0。

- [ ] **Step 4: Commit**

```bash
git add client/src-tauri/Cargo.toml client/src-tauri/src/audio client/src-tauri/src/bin/mic_probe.rs
git commit -m "feat: wasapi mic capture with 20ms block probe tool"
```

---

### Task 11: 音频——WASAPI 播放（事件驱动）+ 自听工具

**Files:**
- Create: `client/src-tauri/src/audio/playback.rs`
- Create: `client/src-tauri/src/bin/loopback_test.rs`
- Modify: `client/src-tauri/src/audio/mod.rs`（`pub mod playback;`）

**Interfaces:**
- Consumes: `MicCapture`（T10）
- Produces:
  - `audio::playback::spawn_player<F>(mut fill: F) -> anyhow::Result<PlayerHandle>`
    - `F: FnMut(&mut [i16]) + Send + 'static`——回调负责**填充**每个播放周期的样本（长度 = 设备缓冲帧数）
    - `PlayerHandle { pub stop: Arc<AtomicBool> }`（置位后播放线程退出；Drop 时自动停）
  - 验证工具：`cargo run --bin loopback_test`（采→放直通自听）

**实现提示**：渲染设备事件驱动样板与 `E:\pro\captions\src-tauri\src\capture\system.rs` 同构（同一套 wait_for_event/`get_available_space_in_frames`/`write_to_device`），差异：`Direction::Render`、预填充 2 个周期的静音、然后事件循环里 `fill(&mut buf)` 再写出。

- [ ] **Step 1: 实现 playback.rs**

要点（实现时展开）：
```rust
pub struct PlayerHandle { pub stop: Arc<AtomicBool> }

pub fn spawn_player<F>(mut fill: F) -> anyhow::Result<PlayerHandle>
where F: FnMut(&mut [i16]) + Send + 'static {
    // 专属线程内：
    // initialize_mta() → get_default_device(&Direction::Render)
    // → initialize_client(48k/Mono/Int16, Shared, AUTOCONVERTPCM|SRC_DEFAULT_QUALITY)
    // → get_audiorenderclient()
    // → get_buffer_size() 得缓冲帧数 B
    // → 预填充：write_to_device(&vec![0i16; B*2]) 两个周期（避免启动爆音/欠载）
    // → set_get_eventhandle() + start_stream()
    // → 循环（stop 未置位）：
    //     wait_for_event() → available = get_available_space_in_frames()
    //     → fill(&mut buf[..available]) → write_to_device(&buf)
    // stop 置位 → stop_stream()
    Ok(PlayerHandle { stop })
}
```

- [ ] **Step 2: loopback_test 工具**

```rust
//! 自听工具：麦克风 → （直接回放）→ 扬声器/耳机。用耳机测试，防啸叫。
fn main() {
    // 采集线程：MicCapture + 累积 960 样本成块 → mpsc::sync_channel<Vec<i16>>(8)
    // 播放：spawn_player(move |out| { try_recv 到块则拷入（余量填 0）；无块填 0 })
    // 运行到用户 Ctrl+C
}
```

- [ ] **Step 3: 验证（务必戴耳机）**

Run: `cargo run --bin loopback_test`
Expected: 对着麦克风说话，耳机中几乎实时听到自己（主观延迟 <100ms）；声音清晰无中断。**注意：扬声器外放会啸叫，必须戴耳机。**

- [ ] **Step 4: Commit**

```bash
git add client/src-tauri/src/audio client/src-tauri/src/bin/loopback_test.rs
git commit -m "feat: wasapi playback with fill callback and loopback self-listen tool"
```

---

### Task 12: 音频——抖动缓冲（每发送者独立）

**Files:**
- Create: `client/src-tauri/src/audio/jitter.rs`（含测试）
- Modify: `client/src-tauri/src/audio/mod.rs`

**Interfaces:**
- Consumes: 无（纯逻辑）
- Produces:
  - `audio::jitter::JitterBuffer::new(target: usize, max: usize) -> JitterBuffer`
  - `insert(&mut self, seq: u32, data: Vec<u8>)`
  - `pop(&mut self) -> PopResult`
  - `enum PopResult { Ready(Option<Vec<u8>>), NotYet }`
    - `Ready(Some(data))`：正常取出一帧
    - `Ready(None)`：该序列号缺帧（调用方用 Opus PLC 解码）
    - `NotYet`：水位未到/仍在等待缺失帧（调用方本次不出声）
  - 常量建议：`target = 3`、`max = 8`、缺帧等待上限 **40ms**（= 2 帧时长）

- [ ] **Step 1: 写失败的测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn f(b: u8) -> Vec<u8> { vec![b] }

    #[test]
    fn fills_watermark_then_pops_in_order() {
        let mut jb = JitterBuffer::new(3, 8);
        jb.insert(0, f(0));
        jb.insert(1, f(1));
        assert_eq!(jb.pop(), PopResult::NotYet, "水位未满不出声");
        jb.insert(2, f(2));
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(0))));
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(1))));
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(2))));
    }

    #[test]
    fn out_of_order_reordered() {
        let mut jb = JitterBuffer::new(2, 8);
        jb.insert(2, f(2));
        jb.insert(0, f(0));
        jb.insert(1, f(1));
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(0))));
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(1))));
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(2))));
    }

    #[test]
    fn missing_frame_triggers_plc_after_wait() {
        let mut jb = JitterBuffer::new(2, 8);
        jb.insert(0, f(0));
        jb.insert(2, f(2));
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(0))));
        // seq=1 缺失：先 NotYet（等待窗口内），超过 40ms 后 Ready(None)（PLC）
        let t0 = std::time::Instant::now();
        let mut plc = false;
        while t0.elapsed() < std::time::Duration::from_millis(200) {
            match jb.pop() {
                PopResult::NotYet => std::thread::sleep(std::time::Duration::from_millis(5)),
                PopResult::Ready(None) => { plc = true; break; }
                PopResult::Ready(Some(_)) => panic!("不应有数据"),
            }
        }
        assert!(plc, "缺失帧应在等待超时后触发 PLC");
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(2))), "PLC 后应继续出后续帧");
    }

    #[test]
    fn overflow_drops_oldest() {
        let mut jb = JitterBuffer::new(2, 4);
        for i in 0..10u32 { jb.insert(i, f(i as u8)); }
        // 上限 4：仅保留最后 4 帧（6..9）
        assert_eq!(jb.pop(), PopResult::NotYet); // 首轮水位（含裁剪后）……见实现注释
        // 实现采取：裁剪保留最大 seq 的 max 帧，且水位判断基于现存帧数
        let mut got = Vec::new();
        while let PopResult::Ready(Some(v)) = jb.pop() { got.push(v[0]); }
        assert!(got.len() <= 4, "超上限应被裁剪: {:?}", got);
    }

    #[test]
    fn late_frame_dropped() {
        let mut jb = JitterBuffer::new(1, 8);
        jb.insert(0, f(0));
        assert_eq!(jb.pop(), PopResult::Ready(Some(f(0))));
        jb.insert(0, f(99)); // 过老的重复帧
        assert_eq!(jb.pop(), PopResult::NotYet);
    }
}
```

（说明：`overflow_drops_oldest` 的"水位+裁剪"组合行为以实现注释为准——实现后如断言与实际语义有冲突，**以"裁剪保留最新 max 帧 + 裁剪后帧数 ≥ target 才出声"为正确语义**调整该测试。）

- [ ] **Step 2: 运行确认失败**

Run: `cargo test`
Expected: 编译失败（JitterBuffer 未定义）

- [ ] **Step 3: 实现（jitter.rs）**

```rust
//! 抖动缓冲：按序列号重排语音帧；水位控制开始播放；缺帧等待后触发 PLC。
use std::collections::BTreeMap;
use std::time::{Duration, Instant};

#[derive(Debug, PartialEq)]
pub enum PopResult {
    Ready(Option<Vec<u8>>),
    NotYet,
}

pub struct JitterBuffer {
    target: usize,
    max: usize,
    frames: BTreeMap<u32, Vec<u8>>,
    next_seq: Option<u32>,
    wait_since: Option<Instant>,
    wait_limit: Duration,
}

impl JitterBuffer {
    pub fn new(target: usize, max: usize) -> Self {
        JitterBuffer {
            target,
            max: max.max(target),
            frames: BTreeMap::new(),
            next_seq: None,
            wait_since: None,
            wait_limit: Duration::from_millis(40), // 2 帧
        }
    }

    pub fn insert(&mut self, seq: u32, data: Vec<u8>) {
        if let Some(next) = self.next_seq {
            if seq < next {
                return; // 过老：丢弃
            }
        }
        self.frames.entry(seq).or_insert(data);
        // 超上限：裁剪最旧的（保留最新 max 帧）
        while self.frames.len() > self.max {
            let oldest = *self.frames.keys().next().unwrap();
            self.frames.remove(&oldest);
        }
    }

    pub fn pop(&mut self) -> PopResult {
        // 初始化水位
        if self.next_seq.is_none() {
            if self.frames.len() < self.target {
                return PopResult::NotYet;
            }
            self.next_seq = self.frames.keys().next().copied();
        }
        let next = self.next_seq.unwrap();
        if let Some(data) = self.frames.remove(&next) {
            self.next_seq = Some(next.wrapping_add(1));
            self.wait_since = None;
            return PopResult::Ready(Some(data));
        }
        // 缺帧：等待窗口内 NotYet；超时触发 PLC
        match self.wait_since {
            None => {
                self.wait_since = Some(Instant::now());
                PopResult::NotYet
            }
            Some(t) if t.elapsed() < self.wait_limit => PopResult::NotYet,
            Some(_) => {
                self.next_seq = Some(next.wrapping_add(1));
                self.wait_since = None;
                PopResult::Ready(None)
            }
        }
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test`
Expected: 5 个测试 PASS

- [ ] **Step 5: Commit**

```bash
git add client/src-tauri/src/audio
git commit -m "feat: per-sender jitter buffer with watermark and plc timing"
```

---

### Task 13: 音频——混音 + 软限幅

**Files:**
- Create: `client/src-tauri/src/audio/mixer.rs`（含测试）
- Modify: `client/src-tauri/src/audio/mod.rs`

**Interfaces:**
- Consumes: `protocol::FRAME_SAMPLES`
- Produces:
  - `audio::mixer::MixAccumulator`：
    - `new(len: usize) -> Self`（len = FRAME_SAMPLES）
    - `add(&mut self, pcm: &[i16])`（累加一路，越界部分忽略——各路长度可不等）
    - `finalize(&self, out: &mut [i16])`（i32 求和 → tanh 软限幅 → i16）
    - `clear(&mut self)`

- [ ] **Step 1: 写失败的测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    const N: usize = 960;

    fn ramp(scale: i16) -> Vec<i16> {
        (0..N).map(|i| ((i as i32 * scale as i32 / N as i32) - scale as i32 / 2) as i16).collect()
    }

    #[test]
    fn silence_plus_signal_equals_signal() {
        let sig = ramp(10000);
        let mut acc = MixAccumulator::new(N);
        acc.add(&vec![0i16; N]);
        acc.add(&sig);
        let mut out = vec![0i16; N];
        acc.finalize(&mut out);
        // 小信号处于近似线性区：输出与输入接近（每样本误差 < 200）
        for (o, s) in out.iter().zip(sig.iter()) {
            assert!((*o as i32 - *s as i32).abs() < 200, "o={o} s={s}");
        }
    }

    #[test]
    fn two_equal_signals_roughly_double_in_linear_zone() {
        let sig = ramp(6000);
        let mut acc = MixAccumulator::new(N);
        acc.add(&sig);
        acc.add(&sig);
        let mut out = vec![0i16; N];
        acc.finalize(&mut out);
        let mid = N / 2;
        let expected = sig[mid] as i32 * 2;
        assert!((out[mid] as i32 - expected).abs() < 400, "out={} expected={}", out[mid], expected);
    }

    #[test]
    fn loud_mix_is_soft_limited() {
        // 4 路满幅同相信号：原始和 4×32767，必须被压回 i16 范围且不翻转符号
        let sig = vec![30000i16; N];
        let mut acc = MixAccumulator::new(N);
        for _ in 0..4 {
            acc.add(&sig);
        }
        let mut out = vec![0i16; N];
        acc.finalize(&mut out);
        for &v in &out {
            assert!(v > 20000, "软限幅后仍应是大信号: {v}");
            assert!(v < 32767, "不得削顶到上限: {v}");
        }
    }

    #[test]
    fn clear_resets() {
        let mut acc = MixAccumulator::new(N);
        acc.add(&vec![1000i16; N]);
        acc.clear();
        let mut out = vec![1i16; N];
        acc.finalize(&mut out);
        for &v in &out {
            assert_eq!(v, 0);
        }
    }

    #[test]
    fn empty_mix_is_silence() {
        let acc = MixAccumulator::new(N);
        let mut out = vec![123i16; N];
        acc.finalize(&mut out);
        assert!(out.iter().all(|&v| v == 0));
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd E:\pro\EchoRoom\client\src-tauri; cargo test`
Expected: 编译失败（MixAccumulator 未定义）

- [ ] **Step 3: 实现（mixer.rs）**

```rust
//! 混音：多路 i16 PCM 逐样本求和（i32 中间态）→ tanh 软限幅 → i16。
//! tanh 映射：y = T * tanh(x / T)，T = 0.8 * 32767 ≈ 26214。
//! 特性：小信号近似线性（无色变），大信号平滑压缩（不爆音、不硬削）。
pub const SOFT_LIMIT_T: f32 = 26214.0;

pub struct MixAccumulator {
    acc: Vec<i32>,
}

impl MixAccumulator {
    pub fn new(len: usize) -> Self {
        MixAccumulator { acc: vec![0i32; len] }
    }

    /// 累加一路（各路长度可不等；超出部分的样本忽略）
    pub fn add(&mut self, pcm: &[i16]) {
        for (a, &s) in self.acc.iter_mut().zip(pcm.iter()) {
            *a += s as i32;
        }
    }

    pub fn clear(&mut self) {
        self.acc.iter_mut().for_each(|a| *a = 0);
    }

    /// 软限幅写出
    pub fn finalize(&self, out: &mut [i16]) {
        for (o, &a) in out.iter_mut().zip(self.acc.iter()) {
            let x = a as f32;
            let y = SOFT_LIMIT_T * (x / SOFT_LIMIT_T).tanh();
            *o = y.clamp(i16::MIN as f32, i16::MAX as f32) as i16;
        }
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test`
Expected: 5 个测试 PASS

- [ ] **Step 5: Commit**

```bash
git add client/src-tauri/src/audio
git commit -m "feat: mixer with tanh soft limiting"
```

---

### Task 14: 客户端——UDP 音频链路 + 端到端整合（语音互通）

**Files:**
- Create: `client/src-tauri/src/net/udp.rs`
- Create: `client/src-tauri/src/audio/session.rs`（四线程编排）
- Modify: `client/src-tauri/src/bridge.rs`（AppState 增加 `audio: Mutex<Option<AudioHandle>>`；登录成功后启动管线）
- Modify: `client/src-tauri/src/net/tcp.rs`（收到 LoginOk 后回调启动音频——通过 Bridge 增加 `emit_login_ok` 或直接调用 `bridge::start_audio`）

**Interfaces:**
- Consumes: `MicCapture`（T10）、`spawn_player`（T11）、`JitterBuffer`（T12）、`MixAccumulator`（T13）、`OpusEnc/OpusDec`（T9）、`protocol::udp`（T3）、`NetCmd`（T8）
- Produces:
  - `net::udp::UdpVoiceHandle { pub stop: Arc<AtomicBool> }`（内部两个线程：发送 + 接收）
    - `spawn_udp_voice(server_addr: String, uid: u16, token: u32) -> anyhow::Result<(UdpVoiceHandle, SyncSender<Vec<i16>>, Receiver<(u16, u32, Vec<u8>)>)>`
      - 返回：句柄 + **PCM 送入口**（采集侧写入）+ **语音事件出口**（播放侧读取：`(from_uid, seq, opus)`）
  - `audio::session::AudioHandle { pub stop: Arc<AtomicBool> }`
  - `audio::session::spawn_audio_pipeline(server_addr: String, uid: u16, token: u32, bridge: Bridge) -> anyhow::Result<AudioHandle>`
  - `bridge::start_audio(app: &AppHandle, uid: u16, token: u32, server_addr: String)`（登录成功后调用；停旧启新）

**线程接线总图（本任务核心）**：

```
采集线程(MicCapture) ──tx_pcm(960样本块)──→ UDP发送线程(OpusEnc→socket)
                                              ↑ sock.try_clone()
UDP接收线程(recv 100ms超时 + 每2s心跳) ──tx_voice((uid,seq,opus))──→ 播放线程
播放线程(spawn_player 回调内)：
  drain tx_voice → HashMap<uid, JitterBuffer>.insert
  对每个 uid：jb.pop()
    Ready(Some) → OpusDec.decode → MixAccumulator.add
    Ready(None) → OpusDec.decode(None)=PLC → add
    NotYet      → 跳过
  finalize → 填满 out（声卡周期）
```

- [ ] **Step 1: 实现 net/udp.rs**

要点（实现时展开为完整代码）：
```rust
pub struct UdpVoiceHandle { pub stop: Arc<AtomicBool> }

pub fn spawn_udp_voice(
    server_addr: String,
    uid: u16,
    token: u32,
) -> anyhow::Result<(UdpVoiceHandle, std::sync::mpsc::SyncSender<Vec<i16>>, std::sync::mpsc::Receiver<(u16, u32, Vec<u8>)>)> {
    let stop = Arc::new(AtomicBool::new(false));
    let (tx_pcm, rx_pcm) = std::sync::mpsc::sync_channel::<Vec<i16>>(16);
    let (tx_voice, rx_voice) = std::sync::mpsc::sync_channel::<(u16, u32, Vec<u8>)>(256);

    let sock = std::net::UdpSocket::bind("0.0.0.0:0")?;
    sock.connect(&server_addr)?; // connect 后可用 send/recv（只与服务器通信）
    let sock_send = sock.try_clone()?;

    // 发送线程：PCM → Opus → UDP
    {
        let stop = stop.clone();
        std::thread::spawn(move || {
            let mut enc = match crate::audio::opus::OpusEnc::new() { Ok(e) => e, Err(e) => { eprintln!("[udp] 编码器创建失败: {e}"); return; } };
            let mut seq: u32 = 0;
            while !stop.load(std::sync::atomic::Ordering::Relaxed) {
                let pcm = match rx_pcm.recv_timeout(std::time::Duration::from_millis(100)) {
                    Ok(v) => v,
                    Err(_) => continue,
                };
                if pcm.len() != echoroom_protocol::FRAME_SAMPLES { continue; }
                match enc.encode(&pcm) {
                    Ok(opus) => {
                        let pkt = udp::encode(uid, seq, &UdpPacket::Voice { opus });
                        let _ = sock_send.send(&pkt);
                        seq = seq.wrapping_add(1);
                    }
                    Err(e) => eprintln!("[udp] 编码失败: {e}"),
                }
            }
        });
    }

    // 接收线程：recv(超时) + 心跳 + 注册
    {
        let stop = stop.clone();
        std::thread::spawn(move || {
            let _ = sock.send(&udp::encode(uid, 0, &UdpPacket::Register { token }));
            let mut buf = [0u8; 2048];
            let mut last_hb = std::time::Instant::now();
            while !stop.load(std::sync::atomic::Ordering::Relaxed) {
                if last_hb.elapsed().as_millis() >= 2000 {
                    let _ = sock.send(&udp::encode(uid, 0, &UdpPacket::Heartbeat));
                    last_hb = std::time::Instant::now();
                }
                sock.set_read_timeout(Some(std::time::Duration::from_millis(100))).ok();
                match sock.recv(&mut buf) {
                    Ok(n) => match udp::decode(&buf[..n]) {
                        Ok((from_uid, seq, UdpPacket::Voice { opus })) => {
                            let _ = tx_voice.try_send((from_uid, seq, opus)); // 满则丢（抗积压）
                        }
                        Ok((_, _, UdpPacket::RegisterAck)) => println!("[udp] 注册成功"),
                        Ok((_, _, UdpPacket::RegisterReject)) => eprintln!("[udp] 注册被拒（token 错误？）"),
                        Ok(_) => {}
                        Err(_) => {}
                    },
                    Err(_) => {} // 超时：继续循环（心跳节拍）
                }
            }
        });
    }
    Ok((UdpVoiceHandle { stop }, tx_pcm, rx_voice))
}
```

- [ ] **Step 2: 实现 audio/session.rs（四线程编排）**

要点：
```rust
pub struct AudioHandle { pub stop: Arc<AtomicBool> }

pub fn spawn_audio_pipeline(
    server_addr: String,
    uid: u16,
    token: u32,
    bridge: crate::bridge::Bridge,
) -> anyhow::Result<AudioHandle> {
    let stop = Arc::new(AtomicBool::new(false));
    let (voice, tx_pcm, rx_voice) = crate::net::udp::spawn_udp_voice(server_addr, uid, token)?;

    // 采集线程：MicCapture → 累积 960 → tx_pcm
    {
        let stop = stop.clone();
        std::thread::spawn(move || {
            let mic = match crate::audio::capture::MicCapture::open() {
                Ok(m) => m,
                Err(e) => { bridge.emit_conn(ConnState::Rejected(format!("麦克风不可用: {e}"))); return; }
            };
            let mut pending: Vec<i16> = Vec::with_capacity(1920);
            while !stop.load(Ordering::Relaxed) {
                if mic.pump(&mut pending).is_err() { break; }
                while pending.len() >= echoroom_protocol::FRAME_SAMPLES {
                    let block: Vec<i16> = pending.drain(..echoroom_protocol::FRAME_SAMPLES).collect();
                    if tx_pcm.try_send(block).is_err() { /* 队列满：丢块 */ }
                }
            }
        });
    }

    // 播放线程：drain voice → jitter → decode → mix → 声卡
    {
        let mut jbs: std::collections::HashMap<u16, crate::audio::jitter::JitterBuffer> = std::collections::HashMap::new();
        let mut acc = crate::audio::mixer::MixAccumulator::new(echoroom_protocol::FRAME_SAMPLES);
        let mut dec = crate::audio::opus::OpusDec::new()?;
        let mut scratch = vec![0i16; echoroom_protocol::FRAME_SAMPLES];
        let player = crate::audio::playback::spawn_player(move |out| {
            // out 为声卡周期样本；按 FRAME_SAMPLES 分段处理，不足补 0
            while let Ok((uid, seq, opus)) = rx_voice.try_recv() {
                jbs.entry(uid)
                    .or_insert_with(|| crate::audio::jitter::JitterBuffer::new(3, 8))
                    .insert(seq, opus);
            }
            let mut filled = 0usize;
            while filled < out.len() {
                let want = (out.len() - filled).min(echoroom_protocol::FRAME_SAMPLES);
                acc.clear();
                for jb in jbs.values_mut() {
                    match jb.pop() {
                        crate::audio::jitter::PopResult::Ready(Some(f)) => {
                            if dec.decode(Some(&f), &mut scratch).is_ok() { acc.add(&scratch); }
                        }
                        crate::audio::jitter::PopResult::Ready(None) => {
                            if dec.decode(None, &mut scratch).is_ok() { acc.add(&scratch); }
                        }
                        crate::audio::jitter::PopResult::NotYet => {}
                    }
                }
                acc.finalize(&mut scratch[..want]);
                out[filled..filled + want].copy_from_slice(&scratch[..want]);
                filled += want;
            }
        })?;
        // 播放句柄的 stop 也挂到总 stop 上（简单做法：线程监控）
        let stop2 = stop.clone();
        std::thread::spawn(move || {
            while !stop2.load(Ordering::Relaxed) { std::thread::sleep(std::time::Duration::from_millis(200)); }
            player.stop.store(true, Ordering::Relaxed);
        });
    }
    let _ = voice; // UdpVoiceHandle：其 stop 由上层统一管理（详见下）
    Ok(AudioHandle { stop })
}
```

（编排细节：`voice.stop` 应与总 `stop` 同步——实现时把 `UdpVoiceHandle.stop` 直接**复用**总 stop（spawn_udp_voice 接受外部传入 stop），避免多套停止旗标。）

- [ ] **Step 3: bridge 集成（登录成功启动/更新音频管线）**

在 `bridge.rs`：
```rust
pub struct AppState {
    pub config: Mutex<Config>,
    pub net: Mutex<Option<NetHandle>>,
    pub audio: Mutex<Option<crate::audio::session::AudioHandle>>,
}

/// 登录成功后：停旧音频管线，按新 uid/token/server 启动
pub fn start_audio(app: &AppHandle, uid: u16, token: u32, server_addr: String) {
    let state = app.state::<AppState>();
    let bridge = Bridge { app: app.clone() };
    let mut slot = state.audio.lock().unwrap();
    if let Some(old) = slot.take() {
        old.stop.store(true, std::sync::atomic::Ordering::Relaxed);
    }
    match crate::audio::session::spawn_audio_pipeline(server_addr, uid, token, bridge) {
        Ok(h) => *slot = Some(h),
        Err(e) => eprintln!("[audio] 管线启动失败: {e}"),
    }
}
```

`net/tcp.rs` 的 `run_session` 收到 `LoginOk` 分支追加：
```rust
// 启动音频链路（编码器/麦克风/播放；失败不影响文字聊天）
crate::bridge::start_audio(&bridge.app, uid, token, addr.clone());
```
（`run_session` 需要把 `addr`（服务器地址）传进来——调整 `run_loop`/`run_session` 签名携带。）

- [ ] **Step 4: lib.rs 注册更新**

`AppState` 初始化加 `audio: std::sync::Mutex::new(None)`。

- [ ] **Step 5: 端到端验证（本机，双向链路）**

Run（终端 1）: `cargo run -p echoroom-server`
Run（终端 2）: `cargo tauri dev`（真客户端，配好昵称；戴上耳机）
Run（终端 3）: `cargo run -p echoroom-server --bin sim_clients -- 127.0.0.1:9000 1 30`
Expected:
- 终端 3：sim 统计**收到 uid=你 的语音包**（数量级 ~50 包/秒）→ **上行链路 ✓**
- 真客户端：服务器转发 sim 的假语音（无法听懂，但**播放回调应有数据流过**）——在播放线程加一次性日志：首次收到某 uid 帧时 `println!("[audio] 首次收到 uid={uid} 的语音")` → 终端 2 应出现该日志 → **下行链路 ✓**
- 真客户端：对着麦克风说话——sim 统计数字持续增长 ✓
- （可选）终止 sim：其统计停止；真客户端无报错

- [ ] **Step 6: Commit**

```bash
git add client/src-tauri/src/net client/src-tauri/src/audio client/src-tauri/src/bridge.rs client/src-tauri/src/lib.rs
git commit -m "feat: udp voice pipeline with capture, encode, jitter, decode and mix"
```

---

### Task 15: 说话状态（VAD + 蓝框）

**Files:**
- Create: `client/src-tauri/src/audio/vad.rs`（含测试）
- Modify: `client/src-tauri/src/audio/session.rs`（采集线程接入 VAD + 上报）、`client/ui/app.js`（蓝框联调细化）

**Interfaces:**
- Consumes: `NetCmd::SetSpeaking(bool)`（T8）
- Produces:
  - `audio::vad::SpeakingDetector::new(enter_rms: f64, exit_rms: f64, hold: Duration) -> Self`
    - `update(&mut self, rms: f64, now: Instant) -> Option<bool>`（状态翻转时返回 Some(新状态)，否则 None）
  - `spawn_audio_pipeline` 签名追加 `tcp_tx: Sender<NetCmd>`；采集线程每 960 块计算 RMS → `detector.update` → 翻转时 `tcp_tx.send(NetCmd::SetSpeaking(on))`
  - 推荐参数：`enter_rms = 800.0`、`exit_rms = 400.0`、`hold = 400ms`

- [ ] **Step 1: 写失败的测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::time::{Duration, Instant};

    #[test]
    fn hysteresis_prevents_flicker() {
        let t0 = Instant::now();
        let mut d = SpeakingDetector::new(800.0, 400.0, Duration::from_millis(400));
        // 小噪声（500：低于进入阈值）不触发
        assert_eq!(d.update(500.0, t0), None);
        assert_eq!(d.update(500.0, t0 + Duration::from_millis(100)), None);
        // 说话：超过进入阈值 800 → 变 true
        assert_eq!(d.update(1200.0, t0 + Duration::from_millis(200)), Some(true));
        // 说话中偶尔低于进入但高于退出阈值（500）：保持 true（不闪断）
        assert_eq!(d.update(500.0, t0 + Duration::from_millis(300)), None);
        // 持续低于退出阈值：hold 窗口内保持 true
        assert_eq!(d.update(200.0, t0 + Duration::from_millis(400)), None);
        assert_eq!(d.update(200.0, t0 + Duration::from_millis(700)), None);
        // 超过 hold → 变 false
        assert_eq!(d.update(200.0, t0 + Duration::from_millis(900)), Some(false));
    }

    #[test]
    fn immediate_switch_without_hold_for_onset() {
        let t0 = Instant::now();
        let mut d = SpeakingDetector::new(800.0, 400.0, Duration::from_millis(400));
        assert_eq!(d.update(1000.0, t0), Some(true), "进入不加延迟");
    }
}
```

- [ ] **Step 2: 运行确认失败 → 实现 → 通过**

```rust
//! 说话检测：能量双阈值 + 退出保持（防蓝框闪烁）。
use std::time::{Duration, Instant};

pub struct SpeakingDetector {
    enter: f64,
    exit: f64,
    hold: Duration,
    speaking: bool,
    below_since: Option<Instant>,
}

impl SpeakingDetector {
    pub fn new(enter_rms: f64, exit_rms: f64, hold: Duration) -> Self {
        SpeakingDetector { enter: enter_rms, exit: exit_rms, hold, speaking: false, below_since: None }
    }

    /// 状态翻转时返回 Some(新状态)；否则 None
    pub fn update(&mut self, rms: f64, now: Instant) -> Option<bool> {
        if !self.speaking {
            if rms >= self.enter {
                self.speaking = true;
                self.below_since = None;
                return Some(true);
            }
            None
        } else {
            if rms >= self.exit {
                self.below_since = None;
                return None;
            }
            match self.below_since {
                None => {
                    self.below_since = Some(now);
                    None
                }
                Some(t) if now.duration_since(t) < self.hold => None,
                Some(_) => {
                    self.speaking = false;
                    self.below_since = None;
                    Some(false)
                }
            }
        }
    }
}
```

（采集线程里 RMS 计算：`(样本平方均值).sqrt()`，窗口 = 一块 960 样本。）

- [ ] **Step 3: 接线 + UI 验证**

- `session.rs`：`spawn_audio_pipeline(..., tcp_tx: Sender<NetCmd>)`；采集线程持有 `SpeakingDetector`，每块：
  ```rust
  let rms = (block.iter().map(|&s| (s as f64).powi(2)).sum::<f64>() / block.len() as f64).sqrt();
  if let Some(on) = detector.update(rms, std::time::Instant::now()) {
      let _ = tcp_tx.try_send(NetCmd::SetSpeaking(on));
  }
  ```
- `bridge::start_audio` 传入 `NetHandle.tx.clone()`（拿不到时用 `Sender<NetCmd>` 从 `state.net` 复制）
- UI 验证：真客户端说话 → 蓝框亮起（自己的卡片）；sim 无法说话（无 VAD）——**双客户端验证**：本机开第二个 `cargo tauri dev` 实例（不同昵称）互说互看蓝框（共享模式麦克风允许多进程采集）

Run（终端 1）: `cargo run -p echoroom-server`
Run（终端 2/3）: 两个 `cargo tauri dev` 实例
Expected: 任一方说话（或对着麦克风出声）→ **双方 UI 中该成员卡片蓝框在 ~0.2s 内亮起、停止说话 ~0.4s 后熄灭**（无快速闪烁）

- [ ] **Step 4: Commit**

```bash
git add client/src-tauri/src/audio client/src-tauri/src/bridge.rs client/ui
git commit -m "feat: voice activity detection with speaking state broadcast"
```

---

### Task 16: 部署、README 与最终验收

**Files:**
- Create: `deploy/echoroom-server.service`（systemd unit）
- Create: `README.md`
- Modify: 按需小修

**Interfaces:**
- Consumes: 全部既有产物
- Produces: 可在 ECS 上常驻运行的服务端；完整项目文档；端到端验收记录

- [ ] **Step 1: 服务端 release 构建**

Run: `cd E:\pro\EchoRoom; cargo build -p echoroom-server --release`
Expected: `target\release\echoroom-server.exe`（单文件，无运行时依赖）

- [ ] **Step 2: 部署到阿里云 ECS（Ubuntu 系示例）**

步骤：
1. 本地构建 **Linux 目标**（ECS 是 Linux）：`rustup target add x86_64-unknown-linux-musl` 或用 WSL/`cross` 构建；**最稳做法**：在 WSL 内 `cargo build --release --target x86_64-unknown-linux-musl`（静态二进制）
2. `scp target/x86_64-unknown-linux-musl/release/echoroom-server root@<ECS_IP>:/usr/local/bin/`
3. 安全组放行：**入方向 TCP 9000** + **入方向 UDP 9000**（两条规则，源 0.0.0.0/0）
4. systemd：
   `/etc/systemd/system/echoroom-server.service`（内容见 `deploy/echoroom-server.service`）：
   ```ini
   [Unit]
   Description=EchoRoom voice chat server
   After=network.target

   [Service]
   ExecStart=/usr/local/bin/echoroom-server 9000
   Restart=always
   RestartSec=2
   StandardOutput=append:/var/log/echoroom-server.log
   StandardError=append:/var/log/echoroom-server.log

   [Install]
   WantedBy=multi-user.target
   ```
5. `systemctl daemon-reload; systemctl enable --now echoroom-server; systemctl status echoroom-server`

- [ ] **Step 3: README.md（中文，延续 LetterSink 文档风格）**

结构：
- 项目简介（单房间实时语音聊天室 + 公屏）
- 架构图（服务器只转发 / 双通道 / 客户端四线程）
- 环境要求（Windows 11 客户端 / 服务器 Linux x64）
- 使用方法（客户端：首次输入昵称与服务器地址；之后开箱即连）
- 服务器部署指南（即 Step 2）
- 协议说明（报文格式与消息清单，引用设计文档）
- 技术栈与结构（workspace 三块）
- 已知限制（戴耳机假设/无加密/6 人上限/无 AEC）
- 声明与致谢（学习用途、隐私说明：音频不落盘不上传第三方、k2-…相关——**注意：EchoRoom 与 LetterSink 不同，没有第三方模型**，致谢 Tauri/wasapi/audiopus 等）

- [ ] **Step 4: 端到端最终验收（ECS + 真客户端）**

验收清单（记录结果）：
| 项 | 操作 | 目标 |
|----|------|------|
| 语音互通 | 两台设备连 ECS，互相说话 | 双向清晰可懂 |
| 延迟 | 说话到对方听到（主观/秒表） | 同城 <300ms |
| 同时说话 | 3 人同时出声 | 混音正常、无爆音 |
| 断线自愈 | 客户端切 Wi-Fi | 15s 内音频恢复、公屏恢复 |
| 成员上限 | 第 7 个客户端登录 | 被拒且提示"房间已满" |
| 蓝框 | 说话/停止 | 亮灭及时、不闪烁 |
| 长跑 | 挂 1 小时 | 内存不涨、无崩溃 |
| 带宽 | ECS 监控 | 6 人时 <1.5 Mbps |

- [ ] **Step 5: 修复验收中发现的问题**（按实际问题逐个处理，小修直接提交）

- [ ] **Step 6: 最终 Commit**

```bash
git add -A
git commit -m "docs: readme; deploy: systemd unit and acceptance checklist"
```

---

## 计划自审记录

- **规格覆盖**：设计 §3 架构→T1/T14；§4 协议→T2/T3；§4.3 心跳→T6/T14；§5.2 音频链路→T9~T13；§5.3 UI→T7/T8/T15；§5.4 容错→T8/T14；§6 服务端→T4~T6；§6.3 部署→T16；§7 测试策略→各任务验证步骤
- **占位符扫描**：无 TBD/TODO；所有步骤含可执行命令或代码
- **类型一致性**：`TcpMessage`/`UdpPacket`/`Room`/`JitterBuffer`/`MixAccumulator`/`OpusEnc`/`OpusDec`/`NetCmd`/`Bridge` 在各任务中名称与签名一致（`PopResult`、`ConnState` 含 `Rejected(String)` 已在 T8 定稿）
